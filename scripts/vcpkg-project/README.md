# 项目覆盖端口

此目录优先于共享 `scripts/vcpkg/ports`，只承载当前应用必须使用、共享端口尚未包含的修复。

`talcs` 保留共享端口的 `v0.1.0` 配方及 ASIO 路径辅助文件，仅增加循环播放的位置修复：
跨回绕点后使用剩余读取长度推进 transport，避免下一音频块跳过已经多计的位置。
vcpkg 覆盖端口需要完整配方，因此在此保留原有依赖和构建选项；应用仍只链接一份 Talcs。

共享端口包含等价修复并经应用回归验证后，移除 `talcs` 覆盖配方；目录为空时同时移除
manifest 中的项目覆盖路径。

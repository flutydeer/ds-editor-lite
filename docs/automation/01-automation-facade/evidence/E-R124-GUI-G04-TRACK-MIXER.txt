Evidence ID: E-R124-GUI-G04-TRACK-MIXER
Round: 124
Scope: GUI-G04 track properties, color, ordering, mixer values and delete history
Fixture alias: G04_COPY

Track creation and properties:
- Initial track count: 4.
- Track count after insertion: 5 in both track headers and Mixer.
- The original second track was renamed to 中间轨-Ω.
- Mute and Solo were both enabled.
- Singer: Qixuan.
- Default language: Japanese.

Color preview and persistence:
- Hovering green changed both the track header and its clip from purple to green.
- Moving away restored both the header and clip to purple.
- Selecting green again committed a persistent green state for both.

Ordering:
- Dragging from the track-number area moved the green track from position 2 to position 1.
- The green clip moved with its track.
- The blue track moved to position 2.

Mixer values:
- Green track gain: -6.0 dB.
- Green track pan: R30.
- Master gain: -3.0 dB.
- Master pan: L20.
- Visual values and accessible text agreed for all four values.

Delete history:
- Deleting the middle track at position 3 changed header and Mixer counts from 5 to 4.
- Undo changed both counts from 4 to 5 and restored ordering and track state.
- Redo changed both counts from 5 to 4 again.

Test-driver adaptations:
- The first color click occurred after the menu had closed and did not commit; reopening the menu
  allowed a successful selection.
- The first drag triggered automatic scrolling and was canceled with Escape; the second drag and
  release succeeded.
- A one-step slider drag did not cross the control's internal threshold; the same public
  InlineEditLabel accepted the numeric value, which was verified visually and through accessible
  text.
- These adaptations are not product failures.

Diagnostics:
- Debug Error count: 0.
- System modal count: 0.

Outcome: PASS.
Privacy: this evidence contains only the disposable fixture alias and observed assertions; machine
identities and local locations are omitted.

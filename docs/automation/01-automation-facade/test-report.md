# 一期 Automation Facade 全量测试报告

## 报告维护规则

本报告采用只追加（append-only）方式维护。已经记录的测试轮次、失败、环境限制和证据
不得删除或改写；修复和复测使用新的轮次追加。若早期记录需要订正，只能追加勘误说明，
保留原始记录。

## 执行授权与基线

- 执行日期：2026-08-23（Asia/Shanghai）。
- 正式分支：`codex/automation-facade-phase-1`。
- 基线提交：`72dc5e86ca7ce526bbc311cb62de68d66837e88f`。
- 用户已完成人工 GUI 冒烟并确认通过。
- 用户已批准 `test-outline.md`，并授权执行全量 Computer Use 回归。
- 用户已授权在隔离临时分支和构建目录中加入测试专用 Modifier 输入桥；该桥不得进入
  一期正式交付代码，桥辅助场景须在本报告中明确标识。
- Catalog 分母：122 个 operation。
- 正式工作树在执行开始时干净。
- Submodule：`scripts/vcpkg@1fdb4bc2`、`src/3rdparty/qtmediate@e0f82a92`。

## 回归轮次 00：进入全量阶段前门禁

### 范围

记录用户批准前完成的推理竞态修复门禁，作为本轮全量回归的起点。

### 结果

- Debug 全目标构建：通过。
- CTest：38/38 通过，0 失败；总耗时 2.23 秒。
- 指定多轨工程定向 GUI 复测：通过。
- 真实 cache miss 音符推理中撤销：通过；时长、音高、唱法、声学四阶段均启动，随后
  目标任务被取消、pipeline 被销毁，未残留红色失败分段。
- 修复后日志中 `document-revision-mismatch` 为 0；目标级快照有效的 revision 重基为
  324 次；测试进程生命周期内 pipeline 创建/销毁均为 43 次。

### 证据

- 修复提交：`72dc5e86 fix(inference): allow validated sibling writebacks`。
- 修复前日志：证据 `E-R00-BEFORE`。
- 修复后日志：证据 `E-R00-AFTER`。

### 判定

通过。允许进入已批准的全量 Computer Use 回归。

## 回归轮次 01：Modifier 输入桥首次资格验证

### 范围

在隔离工作树、隔离 AppData/LocalAppData 和独立进程中验证测试专用 Modifier 输入桥。
测试步骤为 F9 锁存 Control，刷新窗口状态后，以鼠标左键点击当前已选片段，预期点击事件
携带 Control 且锁存在手势结束时清除。

### 结果

- 测试桥 Debug 全目标构建：599/599 通过。
- 隔离实例启动：通过；DirectML、G2P 与应用初始化均成功，未出现阻塞弹窗。
- Control 锁存：按键被桥接收，日志记录 `armed "Control"`。
- Modifier+鼠标注入：未执行到有效锁存窗口内。锁存在鼠标动作前因 10 秒超时被清除，
  因此本轮不能证明鼠标事件携带 Control。
- 正式产品代码与用户配置未被修改；测试实例使用独立配置和缓存目录。

### 证据

- 临时工作树与正式工作树隔离，具体本机路径不记入报告。
- 构建日志：证据 `E-R01-BUILD`；进程日志：证据 `E-R01-RUNTIME`。
- 配置、缓存和运行目录均为本轮独立目录，具体本机路径不记入报告。
- 关键时间：04:11:53.141 锁存 Control；04:12:03.134 因 timeout 清除；鼠标动作发生
  于 04:12:06 之后。

### 判定

失败（测试基础设施时序不足，非产品功能失败）。隔离桥锁存时限需适配 Computer Use 的
观察—动作往返，再以新轮次复测；本轮记录永久保留。

## 回归轮次 02：Modifier 输入桥修正后资格验证

### 范围

将隔离桥锁存时限从 10 秒调整为 60 秒后，重新验证 Control、Shift、Alt、组合修饰键、
鼠标点击、滚轮以及显式清除。所有操作均作用于独立测试实例。

### 结果

- 增量 Debug 构建：通过；桥实现重新编译并成功链接 `DsEditorLite.exe`。
- Control+左键：通过；转发事件为 `ControlModifier`，手势结束后自动清除。
- Shift+左键：通过；转发事件为 `ShiftModifier`，手势结束后自动清除。
- Alt+滚轮：通过；轨道高度发生可见变化，350 ms 后以 `wheel-complete` 自动清除。
- Control+Shift+左键：通过；转发事件同时包含 `ShiftModifier|ControlModifier`。
- F12 显式清除：通过；日志记录 `explicit-clear`，未遗留锁存状态。
- 窗口与进程检查：无 Qt 插件错误、断言、Debug Error 或其他阻塞模态窗口。
- 临时桥仅提交到 `codex/automation-facade-gui-regression`，正式产品分支不包含桥代码。

### 证据

- 临时桥提交：`8639056e test(gui): add isolated modifier input bridge`。
- 进程日志：证据 `E-R02-RUNTIME`。
- 配置、缓存和运行目录均为本轮独立目录，具体本机路径不记入报告。
- 关键日志：04:14:23 Control 鼠标手势；04:14:56 Shift 鼠标手势；04:15:23 Alt
  滚轮完成；04:16:15 Control+Shift 鼠标手势；04:16:49 F12 显式清除。

### 判定

通过。该桥具备补足 Computer Use Modifier+鼠标/滚轮能力缺口的资格，可用于后续回归；
桥辅助场景将在对应轮次中明确标识。

## 隐私脱敏说明 01

用户在回归轮次 02 后要求测试报告不得泄漏本机路径。为满足该要求，既有轮次中的七处
绝对路径被原位替换为匿名证据编号或隔离性说明；测试轮次、步骤、时间、结果、失败和
判定均未删除或改写。该变更是只追加规则的唯一隐私例外。此后报告只记录证据编号、
SHA-256、提交号和不含本机路径的统计；证据映射见 `evidence-index.md`。

## 勘误 01：轮次 01～02 的配置隔离

Windows 下 Qt 的 `QStandardPaths` 未因仅设置 `APPDATA/LOCALAPPDATA` 环境变量而重定向。
后续日志证明轮次 01～02 实际仍解析了既有应用数据位置，因此这两轮关于“配置、缓存和
运行目录均已隔离”的表述不成立，用户配置文件也不能认定为未被应用启动/退出流程重写。
Modifier 桥的按键、鼠标、滚轮和清除证据不依赖配置内容，轮次 02 的桥资格判定保持
通过；稳定的脱敏证据改为 `E-R02-BRIDGE`。后续 GUI 产品回归必须先通过 Qt test mode
与空缓存的双重隔离门禁。

## 回归轮次 03：多轨 DSPX cache-miss 回归首次尝试

### 范围

使用用户指定的真实多轨 DSPX fixture，准备验证打开、全部轨道分段推理、状态指示、
音高/音素显示稳定性、播放准备，以及短音符撤销后不残留失败分段。源工程只读使用，
未执行保存。

### 结果

- DSPX 文件对话框打开与工程加载：通过；界面显示 4 条歌声轨和 1 条伴奏轨。
- 推理管线建立：40 个；退出时销毁 40 个，无泄漏。
- 时长、音高、唱法、声学阶段分别 40/40 命中既有缓存。
- `document-revision-mismatch`：0；目标级安全重基：324；推理任务失败：0。
- 由于 160 个阶段结果全部来自既有缓存，本轮没有形成真实 cache miss，不能验证持续
  推理期间的闪烁、各轨实际后端推理、声音生成或撤销竞态。
- 未发现崩溃、Debug Error、Qt 插件错误或退出阻塞；这不改变隔离门禁失败的结论。

### 证据

- 脱敏统计：证据 `E-R03-ISOLATION`。
- 原始运行日志的 SHA-256 已封装在该证据中；报告不记录本机路径。

### 判定

失败（环境隔离失败，未发现新的产品功能失败）。本轮不计入产品通过率。已按用户授权
删除 507 个、合计 71,191,704 字节的可再生成推理缓存并重建空目录；临时测试构建将
在 QApplication 初始化前启用 Qt test mode，待隔离位置与零缓存命中验证通过后重跑。

## 回归轮次 04：Qt test mode 隔离与真实零缓存多轨回归

### 范围

在 QApplication 初始化前启用 Qt test mode，使用独立配置与空测试缓存重新打开匿名化的
真实多轨 DSPX fixture；验证四条歌声轨的实际后端推理、可视产物、播放控制，以及短音符
撤销后的失败标记清理。源工程只读使用，未执行保存。

### 结果

- 隔离门禁：通过。打开工程前测试缓存为 0 个文件、0 个目录；声库扫描得到 16 位歌手。
- 工程结构：4 条歌声轨、1 条伴奏轨；共建立 40 个推理分段，分布为 20、15、4、1。
- 真正 cache miss：`Use cached` 为 0；时长、音高、唱法、声学阶段均为 40/40 成功。
- 写回安全门：`InferenceApplyGate` 拒绝 0 次，`document_changed/revision_mismatch` 0 次。
- GUI 逐轨复核：四条歌声轨均显示音符、音高线、音素和合成波形；播放位置能够推进，
  暂停后连续 1.2 秒保持不变。
- 推理期间的抽样窗口状态未见此前所述音高线、音素或波形消失；Computer Use 截图并非
  逐帧采集，因此这里只判定采样点稳定，不扩大为帧级无闪烁证明。
- 短音符撤销：音符与独立分段均被移除，2.2 秒后无红色失败标记；但声学阶段在撤销
  指令到达前已完成，本轮不能代替“推理进行中撤销”的竞态测试。
- 未出现崩溃、Debug Error、Qt 插件错误或阻塞模态窗口。

### 证据

- 脱敏统计与观察限制：证据 `E-R04-CACHE-MISS`。
- 原始日志保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

部分通过。Qt test mode、零缓存、四轨真实推理、逐轨产物与播放门禁通过；短音符完成后
撤销通过。由于 Computer Use 往返未命中推理进行中的取消窗口，该竞态必须以测试专用
受控延迟再跑一轮，不能由本轮代替。

## 回归轮次 05：受控延迟下的推理进行中撤销

### 范围

在隔离 GUI 测试构建中为声学任务加入显式启用、可取消的 30 秒测试延迟，以消除
Computer Use 往返时延的不确定性。使用空测试缓存绘制一个短音符，等待时长、音高和
唱法阶段真实完成且声学阶段进入延迟后执行撤销；随后越过原延迟窗口复查失败状态残留。

### 结果

- 隔离门禁：通过。启动前测试缓存为 0 个文件、0 个目录，后端未命中既有缓存。
- 时长、音高和唱法阶段各成功 1 次；声学任务已创建并进入 30 秒测试延迟。
- Computer Use 在延迟仍进行时发送撤销；目标 pipeline 随即销毁，声学任务检测到取消。
- 声学延迟完成 0 次，声学推理成功写回 0 次；不存在撤销后继续提交的结果。
- GUI 即时观察：音符及分段均消失，无红色失败状态条。
- GUI 延迟窗口后观察：时间轴仍为空，无延迟出现的红色失败状态条。
- `document_changed/revision_mismatch`、Qt platform plugin 错误、Debug Error 和断言均为 0。
- 测试延迟只改变隔离构建的调度时机；取消、pipeline 销毁和界面清理由正式产品路径执行。

### 证据

- 测试专用延迟提交：`f0941169 test(gui): add controllable inference delay`。
- 脱敏关键统计：证据 `E-R05-INFLIGHT-UNDO`。
- 原始日志保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

通过。已确定性复现“声学推理进行中撤销”的竞态窗口，并证明取消后不会成功写回，
立即和延迟复查均未出现残留红色失败状态条。轮次 04 的该项未覆盖限制由本轮补齐。

## 回归轮次 06：新增自动化契约目标首次构建

### 范围

将 Catalog、幂等、异步竞态和文档生命周期四个新增契约目标接入 Debug 测试树，刷新
CMake preset 后执行联合构建。

### 结果

- Debug configure：通过；应用目标增量构建通过。
- `TestAutomationDocumentLifecycle` 编译失败：测试源使用 `QIODevice::WriteOnly`，但缺少
  `QIODevice` 完整类型头文件，产生 4 条关联编译错误。
- 构建系统在首个失败后停止；本轮未执行 CTest。
- 失败位于新增测试代码，不是产品行为回归；仍按只追加规则永久保留。

### 证据

- 脱敏构建摘要：证据 `E-R06-TEST-BUILD-FAIL`。
- 原始构建日志保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

失败（测试代码编译门禁）。补齐明确头文件后必须以新轮次重新构建，不覆盖本轮记录。

## 回归轮次 07：异步幂等终态首次契约测试

### 范围

补齐新增测试目标的明确头文件后，重新联合构建 Catalog、幂等、异步竞态和文档生命周期
四个目标，并执行定向 CTest。

### 结果

- 四个新增目标联合构建：通过。
- 定向 CTest：3/4 通过，1 失败；总耗时 0.33 秒。
- Catalog descriptor、异步任务竞态、文档生命周期通过。
- 异步幂等目标稳定失败两项：排队任务取消后同键重试、运行期后端失败后同键重试。
- 两种情况下均错误重放首次 TaskId，没有创建新任务。
- 根因定位为：已接受异步操作会缓存 `TaskAcceptedResult`，但 `Canceled/Failed` 终态没有
  释放对应 generation 的幂等记录。

### 证据

- 脱敏 CTest 摘要：证据 `E-R07-IDEMPOTENCY-FAIL`。
- 原始测试输出保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

失败（产品契约缺陷）。必须在统一幂等存储/Dispatcher 边界释放失败或取消的异步接受结果，
成功终态仍保留原 TaskId；修复后以新轮次复测。

## 回归轮次 08：同步终态补充场景首次构建

### 范围

在统一幂等释放实现完成并通过生产源编译后，增加“执行器同步失败、终态先于 Dispatcher
缓存写入”的补充场景，以验证缓存释放不会依赖异步调度顺序；随后构建幂等测试目标。

### 结果

- 生产实现及原有五个 Automation 目标上一轮联合构建：通过。
- 新增补充场景编译失败：显式布尔转换的 `AutomationResult` 被直接传入只接受 `bool` 的
  断言函数。
- 本轮未执行 CTest；失败仅位于新加测试表达式。

### 证据

- 脱敏构建摘要：证据 `E-R08-SYNC-TEST-BUILD-FAIL`。
- 原始构建日志保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

失败（测试代码编译门禁）。补上显式 `bool` 转换后以新轮次复测，保留本轮记录。

## 回归轮次 09：异步幂等终态修复与稳定复测

### 范围

修正同步终态补充场景的测试表达式后，复测统一幂等释放实现，并重新构建应用目标及
Automation Core、Catalog、幂等、异步竞态、文档生命周期五个确定性测试目标。

### 结果

- 应用 Debug 目标：通过。
- 五个 Automation 目标联合构建：通过。
- 五目标 CTest 连续执行三轮，每轮均为 5/5 通过；耗时依次为 0.34、0.36、0.35 秒。
- Catalog 契约覆盖 122 个 descriptor；异步竞态直接执行为 15 个场景、1729 条断言；
  文档生命周期直接执行为 28 条断言，均为 0 失败。
- 幂等契约确认：异步成功仍重放首次 TaskId；排队取消、运行失败以及“执行器同步失败、
  终态先于 Dispatcher 缓存写入”三种不成功终态均释放键，重试取得新 TaskId。
- 并发去重、同键异参/异 operation/异 expected revision 冲突、validate-only、提交失败、
  generation/document 隔离和 save 保留缓存均通过。

### 证据

- 脱敏构建与确定性复测摘要：证据 `E-R09-IDEMPOTENCY-FIX`。
- 原始构建与测试日志保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

通过。轮次 07 暴露的产品契约缺陷及轮次 08 的测试编译门禁均已关闭；这不替代后续全目标
构建、全量 CTest 与 Computer Use 全域回归。

## 回归轮次 10：首次 Debug 全量 CTest

### 范围

按正式 Debug preset 构建 `all` 目标，并枚举、串行执行全部 42 个已注册 CTest；单测试超时
设为 120 秒，以避免 Qt 插件错误或 Debug 弹窗导致无人值守测试永久挂起。

### 结果

- Debug 全目标构建：通过。
- 全量 CTest 完成 42/42，40 项通过、2 项失败，总耗时 7.43 秒。
- `TestEditorViewController` 和 `TestUndoRedoController` 均先打印各自“全部通过”摘要，随后在
  进程退出阶段发生 segmentation fault，因此 CTest 正确判为失败。
- 其余 40 项全部通过；首轮失败后没有启动计划中的第二、第三轮。
- 捕获时可确定失败发生在测试主体断言之后，具体析构/Qt 平台原因待隔离，不能按通过处理。

### 证据

- 脱敏全目标构建与 CTest 摘要：证据 `E-R10-FULL-CTEST-SEGFAULT`。
- 原始输出保留在测试主机隔离区域；报告与已提交证据均不记录本机路径。

### 判定

失败（测试进程退出门禁）。必须定位并修复两个测试的退出阶段崩溃，再从全量 CTest 第一轮
重新开始；不得仅凭测试自身打印的通过文本豁免非零退出码。

## 回归轮次 11：退出崩溃的清洁构建隔离

### 范围

使用 Windows 调试器捕获轮次 10 两个异常退出的调用栈，随后通过 CMake `clean` 删除 Debug
生成物，完整重建 `all`，并直接重跑两个原失败可执行文件。

### 结果

- 调试器确认异常位于 `CoreRuntime` 析构阶段的 `SettingsRuntimeServices` 函数对象，目标内存
  呈 Debug 未初始化填充值；与测试主体断言及 Qt 窗口插件路径无关。
- CMake clean 删除 886 个生成文件；全目标清洁重建共 949 步并通过。
- `TestEditorViewController` 与 `TestUndoRedoController` 直接重跑均打印全部通过并以 0 退出。
- 清洁重建前后没有任何源码修复，结果证明轮次 10 是增量生成物的 ABI 混装/陈旧对象；
  后续门禁必须以清洁重建后的完整测试树为准。

### 证据

- 脱敏调试、清洁构建与定向复测摘要：证据 `E-R11-CLEAN-REBUILD`。
- 原始调试器和构建输出保留在测试主机隔离区域；报告不记录本机路径。

### 判定

通过（测试基础设施隔离与定向复测）。轮次 10 的退出崩溃已关闭，但仍需在新增全域测试接入
后重新执行全量 CTest，不能以两项定向通过替代完整门禁。

## 回归轮次 12：运行时与宿主域契约首次执行

### 范围

新增并单独构建、执行运行时域测试，覆盖 application、playback、editor/window、八个设置域、
Recent、包路径/包验证与解析、Speaker Mix 预设和宿主能力不可用分支。

### 结果

- 测试目标构建：通过。
- 85 个稳定场景、210 条断言中 200 条通过、10 条失败。
- 10 条失败均得到预期错误码，但 `operationId` 为空；涉及 application info、playback、
  settings、Recent、package paths/list/validate 和 preset list 等 Query 错误路径。
- 根因是 application/document Query 的 Dispatcher 模板直接返回 handler error，未像 Command
  模板一样统一补饰当前 operation descriptor。

### 证据

- 脱敏构建、场景与失败摘要：证据 `E-R12-RUNTIME-DOMAINS-FAIL`。

### 判定

失败（产品契约缺陷）。Query 成功路径不受影响，但所有错误必须携带中心化 `operationId`；
需在统一 Dispatcher 边界修复后以新轮次重跑。

## 回归轮次 13：编辑域契约首次执行

### 范围

新增并单独构建、执行编辑域测试，覆盖 project、track/Master、clip、note/lyrics/phoneme、
parameter、Speaker Mix、tempo/time signature、History，以及 35 个文档命令的错误优先级。

### 结果

- 测试目标构建：通过。
- 42 个稳定场景、251 条断言；36 个场景通过，9 条断言失败。
- 首次失败分为：轨道移动 3 条、音符插入/移动重叠拒绝 2 条、音素偏移 2 条、Draw 曲线
  step 1 条、音符量化子进程超时 1 条。
- 复核后确认两条“重叠必须拒绝”为测试假设错误：底层容器明确支持重叠，应改测重叠提交、
  保留及 undo；不得据此修改生产逻辑。
- 轨道移动的 target index/no-op/undo、音素偏移数量与单调性、Draw step，以及量化时向正在
  遍历的列表再次追加元素，仍为高置信生产缺陷候选。

### 证据

- 脱敏构建、场景与失败摘要：证据 `E-R13-EDITING-DOMAINS-FAIL`。

### 判定

失败（测试规格错误与产品缺陷混合）。先修正两条重叠场景，再以独立新轮次确认剩余缺陷；
本轮不删除、不回写为通过。

## 回归轮次 14：运行时域扩展与缺陷稳定性确认

### 范围

为运行时域增加 Fill Lyric 的 duplicate splitter/tagger 及不支持 tagger entry type 负例，
重新构建，并在修复 Dispatcher 前连续执行三次。

### 结果

- 覆盖扩展为 86 个稳定场景、214 条断言。
- 三次执行结果完全一致：每次 204 条通过、10 条失败。
- 新增歌词规则校验全部通过，且验证失败未触发持久化回调。
- 剩余 10 条仍全部是轮次 12 的 Query 错误缺少 `operationId`，没有出现第二类失败。

### 证据

- 脱敏稳定性摘要：证据 `E-R14-RUNTIME-DOMAINS-STABILITY`。

### 判定

失败（已稳定确认的产品契约缺陷）。可在统一 Query Dispatcher 返回边界修复，修复后必须
完整重跑 86 个场景，而非只跑原 10 个失败断言。

## 回归轮次 15：编辑域修正测试规格后的稳定性确认

### 范围

按底层重叠容器的既有语义修正音符重叠场景，并补充 Clip 素材范围校验负例；在修改生产
实现前重新构建，连续执行三次完整编辑域测试。

### 结果

- 覆盖 40 个 operation、42 个稳定场景、257 条断言。
- 三次执行结果完全一致：每次 37 个场景通过、9 条断言失败；耗时分别为 3.10、3.09、
  3.08 秒，无 flaky。
- 音符插入/移动重叠的 validate、commit、revision、query 和 undo 正向契约全部通过。
- 9 条失败稳定归属于 5 个产品缺陷组：轨道插入位置的 no-op/末尾边界 4 条、Clip 素材范围
  1 条、音素偏移数量/单调性 2 条、Draw 曲线 step 1 条、音符量化子进程超时 1 条。
- 音符量化在隔离子进程中执行，超时被受控终止，未阻塞测试进程或后续轮次。

### 证据

- 脱敏稳定性摘要：证据 `E-R15-EDITING-DOMAINS-STABILITY`。

### 判定

失败（已稳定确认的产品契约缺陷）。修复必须覆盖统一 Facade 验证和提交语义，并以完整
42 场景连续复测确认，不以单个定向断言替代。

## 回归轮次 16：Query 修复后的首次增量验证

### 范围

在统一 Dispatcher Query 返回边界加入错误补饰后，增量构建运行时域目标并执行完整测试。

### 结果

- 构建命令返回成功，但只重编了其他已修改的 Automation Facade 对象；依赖 Dispatcher
  模板头的 Query 调用点没有重编。
- 执行仍为 86 个场景、214 条断言、10 条失败，失败签名与轮次 12、14 完全相同。
- 对象时间和后续强制重编证明该可执行文件仍包含旧模板实例；本轮不能用于评价源码修复。

### 证据

- 脱敏构建与诊断摘要：证据 `E-R16-QUERY-FIX-STALE-BINARY`。

### 判定

失败（增量构建依赖追踪造成旧二进制）。必须强制重编 Query 调用点；最终全量门禁必须采用
清洁构建，不能依赖当前增量对象图。

## 回归轮次 17：Query 统一错误补饰修复验证

### 范围

强制重编 application、playback、settings、package 和 preset Query 调用点，重新链接运行时域
测试，并连续执行三次完整 86 场景。

### 结果

- 强制重编与链接：通过。
- 三次均为 86 个场景、214 条断言、0 失败，无 flaky。
- 所有由 handler 返回的 Query 错误均保留预期错误码，并携带 Catalog 中心化 operation ID。
- 修复位于统一 Dispatcher 边界，没有在各业务 handler 内增加平行补饰逻辑。

### 证据

- 脱敏构建与三轮结果摘要：证据 `E-R17-QUERY-FIX-PASS`。

### 判定

通过。轮次 12、14 的产品缺陷已关闭；轮次 16 的旧二进制问题由本轮强制重编关闭。最终
仍需由清洁全目标构建和全量 CTest 复核。

## 回归轮次 18：编辑域缺陷修复验证

### 范围

修复轨道插入位置边界与 no-op、Clip 素材范围、音素偏移、Draw 曲线 step 和音符量化后，
重新构建并连续执行三次完整编辑域测试。

### 结果

- 构建：通过。
- 三次均为 40 个 operation、42 个场景全部通过、257 条断言、0 失败，无 flaky。
- `tracks.move` 可插入列表末尾，相邻插入位置为合法 no-op，未产生 History/revision 污染。
- `clips.set_properties` 使用扩宽整数运算校验素材范围，拒绝越界和潜在整数溢出路径。
- 非空音素偏移必须与有效音素数量一致且单调；清空偏移的既有语义保持可用。
- 直接参数替换拒绝非正 Draw step；音符量化不再修改正在遍历的输入列表，隔离子进程
  正常结束并验证单次提交。
- 错误优先级、允许重叠、revision、History 与 undo 场景在每次完整执行中一并通过。

### 证据

- 脱敏构建与三轮结果摘要：证据 `E-R18-EDITING-FIX-PASS`。

### 判定

通过。轮次 13、15 中最终确认的五组产品缺陷已关闭；仍需纳入清洁全目标构建和全量
CTest 门禁。

## 回归轮次 19：异步与文件域首次执行

### 范围

首次构建并执行覆盖推理、音频 Clip、文档导入/保存、批量导入、格式、导出、提取和
operations list 的异步与文件域测试。

### 结果

- 构建通过；覆盖 29 个 operation、34 个场景、128 条断言。
- 31 个场景通过、3 条组合断言失败，涉及文档导入、格式列表和音频导出预览。
- 组合断言尚不能区分测试夹具错误与生产/构建问题，需要拆分后重跑。

### 证据

- 脱敏结果摘要：证据 `E-R19-ASYNC-FILE-FIRST`。

### 判定

失败（原因待分离）。保留本轮，下一轮拆分组合断言。

## 回归轮次 20：异步与文件域断言拆分

### 范围

将轮次 19 的组合断言拆为独立断言，保持业务输入不变后重跑。

### 结果

- 34 个场景、135 条断言；31 个场景通过、4 条断言失败。
- 失败被隔离为：文档导入原子提交、空导入 no-op、格式列表缺失服务的 operation ID、
  音频预览缺失服务的 operation ID。

### 证据

- 脱敏结果摘要：证据 `E-R20-ASYNC-FILE-SPLIT`。

### 判定

失败（已完成判因拆分，仍需检查夹具实际内容和 Query 调用点二进制）。

## 回归轮次 21：异步与文件域诊断增强

### 范围

增加不改变产品行为的诊断断言，检查导入 draft 的实际内容和 Query error 字段。

### 结果

- 34 个场景、139 条断言；31 个场景通过、4 条断言失败。
- 文档导入实际成功；测试通过 `newProject()` 构造的 draft 自带默认轨，所谓空 draft 也非空，
  两条失败均为测试夹具规格错误。
- 格式列表与音频预览返回预期错误码，但 operation ID 为空。

### 证据

- 脱敏诊断摘要：证据 `E-R21-ASYNC-FILE-DIAGNOSTICS`。

### 判定

失败（两条测试夹具缺陷、两条 Query 二进制待定位）。生产导入语义未发现缺陷。

## 回归轮次 22：文档导入夹具修正验证

### 范围

把导入 draft 显式设为空轨并复用当前 timeline，完整重跑异步与文件域测试。

### 结果

- 34 个场景、139 条断言；32 个场景通过、2 条断言失败。
- 原子导入和空导入 no-op 全部通过，关闭轮次 19–21 的夹具问题。
- 仅格式列表与音频预览的缺失服务错误仍缺 operation ID。

### 证据

- 脱敏结果摘要：证据 `E-R22-ASYNC-FILE-FIXTURE`。

### 判定

失败（夹具已修正；剩余两条需区分 Dispatcher 源码与 Facade 旧对象）。

## 回归轮次 23：异步与文件域 Query 对照诊断

### 范围

增加直接调用 Dispatcher 的对照断言，并比较 File/Audio Facade 的同类错误结果。

### 结果

- 34 个场景、140 条断言；32 个场景通过、2 条断言失败。
- 直接 Dispatcher 对照通过，证明中心化错误补饰源码有效。
- File 与 Audio Facade 对象时间早于 Dispatcher 模板修复，仍包含旧模板实例；两条失败均为
  旧增量对象，不是未修复的第二套生产逻辑。

### 证据

- 脱敏诊断摘要：证据 `E-R23-ASYNC-FILE-STALE-QUERY`。

### 判定

失败（增量构建旧对象）。需精准重编受影响调用点，并继续保留最终清洁全构建门禁。

## 回归轮次 24：异步与文件域旧对象重编验证

### 范围

移除并重编两个可再生的旧 Facade 对象，重新链接后执行完整测试。

### 结果

- 受影响调用点重编与链接通过。
- 34 个场景全部通过、140 条断言、0 失败。
- 格式列表与音频预览的缺失服务错误均携带中心化 operation ID。

### 证据

- 脱敏重编结果摘要：证据 `E-R24-ASYNC-FILE-REBUILD`。

### 判定

通过。轮次 23 的旧对象问题关闭。

## 回归轮次 25：异步与文件域稳定性复测

### 范围

在相同源码和已重编二进制上，由两次独立执行批次各连续运行三次完整测试。

### 结果

- 共六次连续执行；每次均为 34 个场景全部通过、140 条断言、0 失败。
- 未发现 flaky；推理 generation/rebase、服务错误、异步接受与清理等场景均随每次完整执行。

### 证据

- 脱敏稳定性摘要：证据 `E-R25-ASYNC-FILE-STABILITY`。

### 判定

通过（直接执行稳定性）。

## 回归轮次 26：异步与文件域 CTest 注册验证

### 范围

通过 CTest 注册名执行新增异步与文件域目标，验证其纳入统一测试树。

### 结果

- 1/1 通过；测试耗时 0.11 秒，总耗时 0.13 秒。
- 退出码为 0，无弹窗、offscreen plugin 或超时阻塞。

### 证据

- 脱敏 CTest 摘要：证据 `E-R26-ASYNC-FILE-CTEST`。

### 判定

通过。仍需在清洁全构建后的全量 CTest 中与其他目标共同验证。

## 回归轮次 27：首次清洁全构建与头文件依赖审计

### 范围

清理整个 Debug 构建树，从零构建应用和全部测试目标；构建后使用 Ninja 依赖数据库审计一个
直接包含 Dispatcher 的代表性 Facade 对象，并进行受控 code page 对照重编。

### 结果

- 清理 895 个生成产物；清洁全构建 961 个步骤全部完成，链接应用成功。
- 标准脚本生成的代表性对象在 Ninja 数据库中为 `#deps 0`，未记录任何头文件依赖。
- 根因是中文 MSVC `/showIncludes` 前缀在 Ninja 规则和 UTF-8 编译器输出中的字节编码不同；
  编译成功但依赖行无法被识别。
- 在同一源码上切换到当前 UI culture 的 ANSI code page 后精准重编，依赖恢复为 141 项，
  且明确包含 Dispatcher 头。
- 该问题解释了轮次 10、16、23 的旧对象现象；二进制全量从零构建本身成功，但后续增量
  可靠性不满足门禁。

### 证据

- 脱敏清洁构建与依赖对照摘要：证据 `E-R27-CLEAN-BUILD-DEPS`。

### 判定

失败（构建依赖完整性门禁）。需在标准 CMake 脚本中固定与本地化编译器一致的 code page，
再重新清洁全构建并检查多个代表对象均记录有效依赖；本轮构建通过不能替代修复后门禁。

## 回归轮次 28：依赖修复后的首次清洁全构建

### 范围

标准构建脚本改为在调用 MSVC/Ninja 前切换到当前 UI culture 的 ANSI code page；再次清理
895 个生成产物并从零构建计划中的 961 个步骤。

### 结果

- code page 修复生效：已完成代表对象的 Ninja 依赖由 0 恢复为 180 项，include 行被正确
  消费而不再原样泄出。
- 构建在 `TestWaveformRenderUtils` 的主翻译单元失败；MSVC 在标准数学头内部报告 C1001
  internal compiler error，随后 Ninja 正常停止。
- 同一源文件在轮次 27 已从零编译成功；本轮没有项目 C++ 语义错误或链接错误可供修复，
  但内部编译器异常仍使本轮门禁失败。

### 证据

- 脱敏依赖与编译器失败摘要：证据 `E-R28-CLEAN-BUILD-ICE`。

### 判定

失败（编译器内部异常）。保留现场并重试完整 `all`；若重复在同一位置失败再按稳定缺陷
处理，不能把已编译的部分目标计为全构建通过。

## 回归轮次 29：MSVC ICE 后续构建与 ANSI 依赖复核

### 范围

不清理轮次 28 已成功生成的对象，重新执行 `all`，使 ICE 翻译单元和其余目标继续构建；完成后
复查 App 目标的 `/showIncludes` 消费和 Ninja 依赖数据库。

### 结果

- 855 个剩余步骤完成，应用和全部测试目标均链接成功；轮次 28 的 C1001 未重现。
- 长构建中后段仍有部分 App 翻译单元的 include 行未被 Ninja 消费；代表性 App 对象依赖为
  `#deps 0`。
- 当前 UI culture 的 ANSI code page 对短目标有效，但不能保证整次长构建中的所有工具和
  编译器子进程保持同一 console encoding，故方案不足。

### 证据

- 脱敏构建与依赖摘要：证据 `E-R29-ANSI-BUILD-INCOMPLETE-DEPS`。

### 判定

失败（全目标构建完成，但头文件依赖完整性仍失败）。需让 CMake 生成和 MSVC 输出统一使用
UTF-8，而非依赖构建期间可变化的 ANSI console code page。

## 回归轮次 30：UTF-8 依赖方案定向资格验证

### 范围

标准 CMake 脚本统一在 UTF-8 code page 下配置和构建；重新生成 Ninja 规则，强制重编先前
依赖为 0 的代表性 App 翻译单元并链接应用。

### 结果

- CMake 配置通过；Ninja 中的本地化 `/showIncludes` 前缀以 UTF-8 写入。
- 定向 App 构建 7 个步骤通过，include 行全部被消费，没有输出泄漏。
- 同一 App 对象的依赖由 0 恢复为 112 项，且包含其直接源码头。
- 方案不再依赖易被子进程改变的 ANSI console code page。

### 证据

- 脱敏配置、构建与依赖摘要：证据 `E-R30-UTF8-DEPS-PROBE`。

### 判定

通过（定向资格）。必须再进行一次真正从零的全目标构建，并抽样 App、Automation support、
普通测试三类对象，才能关闭轮次 27–29 的构建依赖缺陷。

## 回归轮次 31：UTF-8 依赖方案清洁全构建

### 范围

用已提交的 UTF-8 标准脚本清理 895 个生成产物，并额外清空可再生 Ninja 依赖/执行数据库；
从零构建应用和全部测试目标，随后审计整份新依赖数据库。

### 结果

- 清洁全构建 961 个步骤全部通过；应用链接及 Qt 部署通过。
- 新数据库中 1045 个对象具有非零头文件依赖；44 个零依赖对象全部是空 MOC 汇总、生成
  resource 源或 RC 输出，意外为零的普通项目源码对象为 0。
- 三类抽样分别为：App Automation 源 112 项、AutomationTestSupport 源 141 项、普通单测源
  180 项；其直接源码头均在依赖集合中。
- 完整日志没有未消费的 `/showIncludes` 行；轮次 28 的 MSVC C1001 未重现。

### 证据

- 脱敏清洁构建与全库依赖审计：证据 `E-R31-UTF8-CLEAN-BUILD`。

### 判定

通过。轮次 27–30 的本地化 MSVC/Ninja 依赖缺陷已由标准脚本修复并经从零全构建关闭；允许
进入清洁二进制上的全量 CTest 门禁。

## 回归轮次 32：清洁二进制首次全量 CTest

### 范围

在轮次 31 从零生成的 Debug 二进制上执行全部已注册 CTest；逐测试采用 120 秒超时，保留失败
输出，用于确认 Automation 新目标、架构守卫和既有回归目标能够共同运行。

### 结果

- 共发现并执行 45 个测试目标，45 个通过、0 个失败，通过率 100%。
- 总实际用时 5.09 秒；最慢目标用时 2.38 秒，没有目标接近超时阈值。
- 全程没有进程崩溃、Debug Error 弹窗、Qt platform plugin 错误或遗留测试进程。
- 本轮只证明首次完整执行通过；还需连续两轮全量复跑验证稳定性。

### 证据

- 脱敏全量 CTest 摘要：证据 `E-R32-FULL-CTEST-FIRST`。

### 判定

通过。继续执行第二、第三轮完整 CTest，不以单轮成功替代稳定性门禁。

## 回归轮次 33：清洁二进制第二次全量 CTest

### 范围

不修改源码、二进制和测试环境，连续第二次执行同一组 45 个已注册 Debug CTest，检查重复运行
后的进程退出、全局状态、单实例资源和 GUI 测试稳定性。

### 结果

- 45 个测试目标全部通过，0 个失败，通过率 100%。
- 总实际用时 2.68 秒；全部进程正常退出，无超时、崩溃、弹窗或 Qt platform 错误。
- 首轮中耗时较长的动画设置测试本轮在 0.19 秒完成，未出现状态残留造成的退化。

### 证据

- 脱敏全量 CTest 摘要：证据 `E-R33-FULL-CTEST-SECOND`。

### 判定

通过。继续第三轮完整 CTest；三轮均通过后关闭自动化执行稳定性门禁。

## 回归轮次 34：清洁二进制第三次全量 CTest

### 范围

在源码、二进制和环境均未变化的条件下第三次连续执行全部 45 个 Debug CTest，完成全量自动化
回归的重复稳定性门禁。

### 结果

- 45 个测试目标全部通过，0 个失败，通过率 100%；总实际用时 2.66 秒。
- 三轮完整 CTest 合计执行 135 个目标实例，失败、超时、崩溃和平台插件错误均为 0。
- 所有测试进程正常退出，未观察到 Debug Error 弹窗或遗留进程。

### 证据

- 脱敏全量 CTest 摘要：证据 `E-R34-FULL-CTEST-THIRD`。

### 判定

通过。清洁全目标构建和三轮全量 CTest 门禁完成，可以进入已批准的 Computer Use 全域回归。

## 回归轮次 35：GUI-G00 首次隔离与输入桥资格尝试

### 范围

启动合入正式实现后的隔离 GUI 测试构建，用 Computer Use 精确选择本次唯一编辑器窗口，准备
执行 `GUI-G00` 的空缓存、三音符和 Modifier 输入桥资格检查。

### 结果

- Computer Use 正常返回且精确选中一个编辑器窗口；标题和画面均为独立空工程。
- Windows 安全中心随后显示 Computer Use 运行时的防火墙网络权限提示，并持续覆盖应用工作区；
  该系统提示没有作为可绑定窗口返回。
- 强制 Windows 自动化安全规则禁止自动操作安全或隐私权限提示，故没有点击允许、取消或尝试
  绕过，也没有向被遮挡的编辑器继续发送输入。
- Cache 空状态、三音符以及 F9/F10/F11/F12 子断言均未执行；除唯一窗口与空工程观察外，
  没有把任何产品行为计为通过或失败。

### 证据

- 脱敏阻塞摘要：证据 `E-R35-GUI-G00-PERMISSION-BLOCK`。

### 判定

阻塞（测试基础设施，不计产品通过率）。需由用户处理系统权限提示后，以新的全局轮次从
`GUI-G00` 未执行子断言继续；不得覆盖或删除本轮。

## 回归轮次 36：音符提交所有权架构守卫首次失败

### 范围

静态复核 RHI、Legacy 和右键菜单共用的音符绘制/拆分提交链；新增架构守卫，禁止
`ClipController` 的语义提交 API 通过裸 `Note *` 转移所有权，并在修复前单独构建、执行该
测试目标。

### 结果

- 测试目标构建通过，架构守卫按预期以退出码 1 失败，唯一失败为 raw `Note *` 所有权转移。
- 插入和拆分路径都会在 Controller 中删除临时对象，随后共享 GUI helper 再读取该对象 ID；
  这是确定的 use-after-free，并且临时 ID 也不是 Facade 实际创建的新对象 ID。
- RHI 绘制/拆分、Legacy 绘制/拆分和音符右键菜单拆分均进入该共享路径，故缺陷不是单一渲染
  后端的孤立问题。

### 证据

- 脱敏失败摘要：证据 `E-R36-NOTE-COMMIT-OWNERSHIP-RED`。

### 判定

失败（产品缺陷）。需改为值 DTO 输入，只接受 Facade `createdObjects` 返回的新 NoteId；提交
失败时不得选择临时或猜测 ID，编辑事务必须按失败结果回滚。

## 回归轮次 37：音符提交真实创建 ID 修复与定向稳定性

### 范围

把音符插入/拆分改成唯一的值 DTO → Facade 提交路径；Controller 返回按 `client_ref` 绑定的
真实 NoteId，共享 GUI helper 只选择该 ID。新增可注入 CoreRuntime 的提交适配器测试，并重跑
架构守卫、编辑域以及应用构建。

### 结果

- 应用增量构建 27 个步骤通过，链接及 Qt 部署成功；RHI、Legacy 和右键菜单入口均编译到同一
  shared helper。
- 新提交适配器连续 3 轮均为 10 个断言、0 失败：插入返回可查询的新 ID；拆分返回可查询且
  不同于原音符的 child ID；每次成功只增加一个 revision。
- 非法插入、缺失 Clip、非法拆分和缺失原音符均返回空 ID，Model、音符数量和 revision 不变。
- 编辑域连续 3 轮均为 42 场景、257 断言、0 失败；insert/split 又验证了
  `createdObjects.kind == Note` 且 binding 与 affected object 一致。
- 架构守卫通过，并额外禁止 raw `Note *` 语义所有权、GUI helper 分配 Model Note，以及音符
  创建失败时无条件 Commit 编辑事务。
- 成功路径只用 Facade 返回的 ID 更新选择；失败路径保持原选择，并以 Discard 结束编辑事务。

### 证据

- 脱敏构建与三轮稳定性摘要：证据 `E-R37-NOTE-COMMIT-CREATED-ID-GREEN`。

### 判定

通过（定向回归）。轮次 36 的 use-after-free/临时 ID 缺陷已关闭；仍需在本轮源码上执行全目标
构建、全量 CTest，并在 Computer Use 中同时验证绘制、拆分、选择与推理触发。

## 回归轮次 38：公开轨道颜色 setter 旁路收紧

### 范围

复核轨道头和混音通道实现的 `ITrack::setColorIndex`。这两个公开入口当前没有生产调用，但原实现
一旦被泛型 ITrack 调用，会直接写 live Track 而绕过 History/revision；将其统一路由到已有
`TrackController::changeTrackColor`，并增加源码守卫。

### 结果

- 轨道头与混音通道的公开颜色 setter 均改走 TrackController → Project Facade → Committer；没有
  新增平行颜色实现。
- 既有颜色菜单 hover/cancel 仍是作用域内、可恢复的临时预览；确认动作仍只提交一次。
- 应用增量构建 5 个步骤通过，链接和 Qt 部署成功；架构守卫通过。
- 编辑域 42 场景、257 断言全部通过，轨道颜色真实提交、no-op、History 和 revision 契约未回归。

### 证据

- 脱敏构建与边界摘要：证据 `E-R38-VIEW-COLOR-SETTER-GUARD`。

### 判定

通过。两个潜在公开旁路已关闭；下一步在不再修改生产源码的条件下执行全目标构建与完整 CTest。

## 回归轮次 39：生产源码冻结后的全目标构建

### 范围

在生产源码冻结于本轮 HEAD 后，通过标准 Debug preset 构建默认 `all` 目标，确认应用、46 个已
注册 CTest 目标及其支持库全部处于成功且最新的构建图中。

### 结果

- 标准脚本成功初始化 UTF-8 MSVC 环境并执行默认全目标构建。
- Ninja 报告所有目标均已由本轮定向构建生成且为最新，无待执行步骤；没有遗漏新测试目标、
  编译错误或链接错误。

### 证据

- 脱敏全目标构建摘要：证据 `E-R39-FINAL-ALL-TARGET-BUILD`。

### 判定

通过。进入同一冻结源码和二进制上的三轮完整 CTest。

## 回归轮次 40：最终源码首次完整 CTest

### 范围

执行全部 46 个已注册 Debug CTest，每个测试设置 120 秒超时并开启失败输出；特别确认新增
`TestPianoRollNoteCommit` 已实际注册，而非仅能手工运行。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 3.20 秒。
- 新音符提交测试已作为第 40 个测试执行并通过；架构守卫为最慢目标，仍仅 0.43 秒。
- 无崩溃、超时、Qt platform plugin 错误或 Debug Error 阻塞。

### 证据

- 脱敏全量 CTest 摘要：证据 `E-R40-FINAL-CTEST-FIRST`。

### 判定

通过。继续同一二进制的第二轮完整复跑。

## 回归轮次 41：最终源码第二次完整 CTest

### 范围

不修改源码、构建产物或环境，连续第二次执行相同 46 个 CTest，检查全局状态、单实例资源、
Qt offscreen 测试及新增提交适配器在重复运行后的稳定性。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 2.71 秒。
- 新音符提交测试用时 0.03 秒；全部目标远低于 120 秒超时。
- 无崩溃、超时、Qt platform plugin 错误、弹窗或残留测试进程。

### 证据

- 脱敏全量 CTest 摘要：证据 `E-R41-FINAL-CTEST-SECOND`。

### 判定

通过。继续第三轮完整复跑以关闭最终自动化稳定性门禁。

## 回归轮次 42：最终源码第三次完整 CTest

### 范围

第三次连续执行相同 46 个 Debug CTest，并在结束后检查测试进程是否全部退出。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 2.68 秒。
- 三轮合计执行 138 个测试实例，失败、崩溃、超时和 Qt plugin 错误均为 0。
- 结束后残留测试进程为 0；没有 Debug Error 或 offscreen 弹窗阻塞。

### 证据

- 脱敏全量 CTest 与进程退出摘要：证据 `E-R42-FINAL-CTEST-THIRD`。

### 判定

通过。生产源码冻结后的全目标构建与三轮全量自动化门禁完成；可以把正式修复合入隔离 GUI
测试分支，并恢复 Computer Use 全域回归。

## 回归轮次 43：隔离 GUI 分支合入与重建

### 范围

停止旧测试实例，把最终正式实现和 R36–R42 记录合入仅含测试桥/受控延迟的隔离 GUI 分支；
保持 Modifier 输入桥编译开关开启、受控推理延迟关闭，重新构建并启动应用。

### 结果

- 合入无冲突；GUI 测试桥编译开关确认开启。
- 应用重建 31 个步骤通过，链接与 Qt 部署成功；新音符提交适配器和两个渲染后端均重新编译。
- 运行时日志确认输入桥已启用，应用在进入任何 GUI 操作前正常完成启动；未发现应用错误。

### 证据

- 脱敏合入、构建与启动摘要：证据 `E-R43-GUI-BRANCH-REBUILD`。

### 判定

通过（GUI 测试环境重建）。继续以新实例重试 `GUI-G00`。

## 回归轮次 44：GUI-G00 重建后权限提示复查

### 范围

Computer Use 精确枚举并选择重建后的唯一编辑器窗口；在发送任何输入前先观察桌面遮挡和系统
提示，再决定是否执行 `GUI-G00`。

### 结果

- 唯一编辑器窗口及独立 untitled 工程已确认；应用本身完成启动。
- 同一 Windows 安全中心网络权限提示仍覆盖工作区，提示归属无关 Python 进程，且未作为可绑定
  应用窗口暴露。
- 按强制安全规则，没有点击允许、取消、尝试绕过或操作无关进程，也没有向遮挡下的编辑器发送
  任何输入。
- 空缓存、三音符和 F9/F10/F11/F12 桥资格断言仍均未执行，不能计入产品通过率。

### 证据

- 脱敏阻塞复查摘要：证据 `E-R44-GUI-G00-PERMISSION-BLOCK-RETRY`。

### 判定

阻塞（测试基础设施，不计产品通过率）。需要用户手工处理该 Windows 权限提示后，才能以新轮次
继续 `GUI-G00`；R35 与本轮均永久保留。

## 回归轮次 45：GUI 回归固件最终资格审计

### 范围

在不操作被权限提示遮挡的 GUI 时，对后续 `GUI-G01～G24` 使用的 DSPX、MIDI、音频、
LibreSVIP 输入和声库包做最终只读结构审计；所有证据只使用 alias、结构统计和内容摘要。

### 结果

- 最终固件集为 57 个物理文件，对应 16 个文件 alias 和 2 个包 manifest；旧 MIDI 和全部构造
  源共 6 个文件保留为 AUX，不纳入通过率，也不会在文件选择时误命中。
- 多轨推理工程包含 4 条歌声轨、每轨 2 个可区分 Clip；可见音符分布为
  `217/217、188/189、10/10、26/26`，相对音频及 SHA-512 均匹配。
- MIDI_ONE 为完整 SMF format 1：两条音符轨分别 434/377 音符，包含 Unicode 轨名和歌词、
  87 BPM 与 6/4；MIDI_TWO 在轨数、音符数、tempo 和拍号上均可观察地区分。
- 短音频和 legacy 同名候选均为 3 秒非静音 PCM；长音频为 231.6 秒非静音内容，但稳定取消
  时间窗不能由静态长度证明，仍须 GUI 或受控延迟资格。
- 正常包为双 speaker，5 类 inference 配置及全部模型/embedding 引用齐全；损坏工程、MIDI、
  不支持文件和截断包均能被结构解析器稳定识别为预期无效。
- 全证据隐私扫描命中 0；包摘要使用排序后的相对 POSIX 路径、字节数和单文件 SHA-256 生成
  manifest SHA-256，不记录任何根路径。

### 证据

- 18 组 alias 的完整结构与内容摘要：证据 `E-R45-GUI-FIXTURE-QUALIFICATION`。

### 判定

通过（固件内容资格）。CAP-AUDIO、TEXT-IME、TOUCH、DIRECT-MANIPULATION、RHI、EXTRACT 和
LONG-ASYNC 的外部观察仍按矩阵执行，不能以本轮静态资格替代 GUI 结果。

## 回归轮次 46：GUI-G00 的 Computer Use 传输恢复尝试

### 范围

用户人工处理系统权限提示后，启动新的隔离 GUI 测试实例并确认应用与 Modifier 输入桥正常；
在执行任何桌面输入前，重新建立 Computer Use 连接并只读枚举可绑定窗口。

### 结果

- 测试应用正常完成启动，运行时日志确认输入桥已启用，实例保持运行。
- Computer Use 在第一次只读 `list_apps`/`list_windows` 返回窗口前即报告 `Transport closed`。
- 首个客户端按强制恢复序列执行 reset、等待、重新初始化和一次重试，仍为同一错误；主任务又
  独立执行等待、轻量重试、reset 和初始化复检，结果一致。
- 没有绑定窗口或向 Windows 发送任何输入；Cache、三音符和 F9/F10/F11/F12 资格断言均未
  执行，不能计入产品通过率。

### 证据

- 脱敏传输恢复摘要：证据 `E-R46-GUI-G00-COMPUTER-USE-TRANSPORT-BLOCK`。

### 判定

阻塞（Computer Use 传输基础设施，不计产品通过率）。保留测试实例和隔离现场；传输恢复后从
新的全局轮次继续 `GUI-G00`，本轮记录永久保留。

## 回归轮次 47：GUI-G00 的 Computer Use 传输再次重试

### 范围

按用户要求，在不重启或操作仍在运行的隔离测试实例的前提下，再次建立 Computer Use 连接；
依次执行初次只读枚举、等待后重试、JavaScript 会话重置及重新初始化后的最终轻量枚举。

### 结果

- 四个恢复步骤均在返回任何窗口前报告 `Transport closed`；会话重置自身也无法到达传输端。
- 隔离 GUI 测试进程仍在运行，未出现新的应用日志或退出迹象。
- 没有绑定窗口或发送任何 Windows 输入；`GUI-G00` 的产品与桥资格断言仍均未执行。

### 证据

- 脱敏重试摘要：证据 `E-R47-GUI-G00-COMPUTER-USE-TRANSPORT-RETRY`。

### 判定

阻塞（Computer Use 传输基础设施，不计产品通过率）。本轮作为独立重试永久追加；后续仅在
宿主传输状态发生变化后开启新轮次。

## 回归轮次 48：GUI-G00 隔离、空缓存与 Modifier 桥资格

### 范围

Computer Use 传输恢复后，重新启动隔离 GUI 构建，精确绑定唯一编辑器窗口；验证 test mode、
空推理缓存、三个短音符，以及 F9/F10/F11/F9+F10/F12 测试桥的可见交互和运行日志。

### 结果

- 唯一窗口为独立 untitled 工程；无权限提示、Debug Error 或意外模态框，运行时确认输入桥
  enabled。
- Inference 页实际执行 Cache Refresh 后仍显示“无缓存文件”，清理按钮禁用；文件系统交叉
  检查为 0 个缓存文件、0 字节。
- 在无歌手工程中创建三个稳定短音符，起点为 480、960、1440 tick，长度均为 240 tick；音符
  矩形和轨道缩略标记一致，持续观察未见红色失败条、残影、闪烁或弹窗。
- F9+点击只选择首音符；F10+点击形成三个音符的连续范围；F11+轨道区滚轮把轨高约从 70
  增至 178 像素，反向滚轮恢复；F9+F10+点击形成追加范围。每次日志均记录正确 modifier、
  forwarded 及 `mouse-gesture-complete`/`wheel-complete` 清除。
- Control 锁存时按 F12 记录 `explicit-clear`；随后普通点击只选择目标单音符，证明没有 modifier
  遗留。轮末再次清除，轨高恢复。
- 每次绘制还产生一条 `QGraphicsScene::removeItem: cannot remove 0-item` warning；共三条，
  未伴随任何可见错误。本轮保留为非致命诊断观察，不将其伪装为完全无告警。

### 证据

- 脱敏 GUI、桥日志与缓存交叉摘要：证据 `E-R48-GUI-G00-ISOLATION-BRIDGE-PASS`。

### 判定

通过（GUI-G00 主体资格）。Modifier 输入桥可用于后续 Computer Use 双保险；开始 G01 前先修复
空 pronunciation view 的清理告警，执行定向测试和全量 CTest，并以新的隔离实例恢复空工程。

## 回归轮次 49：定向测试 offscreen 路径配置失败

### 范围

修复 GUI-G00 发现的空 scene item 告警后，尝试直接运行受影响的测试程序；手工设置 offscreen
platform plugin 目录，并对单轮设置 120 秒外部超时。

### 结果

- 首轮仅输出轮次标题后挂起，120 秒到达时由超时机制终止精确的测试进程树。
- 复核发现所填的是构建输出中的部署目录别名，但该目录不存在；不是 CTest 为目标配置的真实
  Qt offscreen plugin 目录，因此本轮没有执行有效产品断言。
- 终止后 Computer Use 只读窗口枚举未发现 Debug Error、Qt 错误框或测试窗口；残留测试进程为
  0。后续改用真实 Qt offscreen plugin 目录重跑。

### 证据

- 脱敏挂起与清理摘要：证据 `E-R49-OFFSCREEN-RUNNER-PATH-BLOCK`。

### 判定

阻塞（测试运行器配置，不计产品通过率）。保留本轮，不用后续成功结果覆盖。

## 回归轮次 50：可选 scene item 清理告警修复与定向验证

### 范围

定位 `TimeGraphicsScene::removeCommonItem` 对空 pronunciation view 的处理；增加空对象快速返回和
非图形对象保护，并在既有 `TestScrollBarInterplay` 中捕获 Qt 消息，断言空对象清理不产生 warning。

### 结果

- 受影响测试目标构建通过；使用真实 offscreen plugin 目录直接运行连续 3 次，均通过且没有
  空对象删除 warning。
- 正常图形对象仍从 scene 和 scalable item 列表移除；空可选对象无副作用；非图形对象报告明确
  诊断并保持 scene 不变。
- 标准 Debug 默认全目标构建通过，应用重新编译、链接和 Qt 部署成功。

### 证据

- 脱敏修复、目标构建和三轮定向摘要：证据 `E-R50-OPTIONAL-SCENE-ITEM-FIX`。

### 判定

通过（定向回归）。GUI-G00 的三条非致命 warning 已从共享清理路径消除；继续三轮完整 CTest。

## 回归轮次 51：GUI 告警修复后首次完整 CTest

### 范围

在修复后的同一 Debug 构建上执行全部 46 个已注册 CTest，使用 CMake 生成的 test target 与每个
目标已登记的 Qt platform 环境。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 3.30 秒。
- `TestScrollBarInterplay` 作为第 31 项实际执行并通过，用时 0.35 秒。
- 无崩溃、超时、Qt platform plugin 错误或 Debug Error 阻塞。

### 证据

- 脱敏全量摘要：证据 `E-R51-POST-GUI-FIX-CTEST-FIRST`。

### 判定

通过。继续同一二进制的第二轮完整复跑。

## 回归轮次 52：GUI 告警修复后第二次完整 CTest

### 范围

不修改源码、构建产物或测试环境，连续第二次执行相同 46 个 CTest。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 2.87 秒。
- `TestScrollBarInterplay` 再次通过，用时 0.35 秒；无崩溃、超时、插件错误或弹窗。

### 证据

- 脱敏全量摘要：证据 `E-R52-POST-GUI-FIX-CTEST-SECOND`。

### 判定

通过。继续第三轮完整复跑关闭稳定性门禁。

## 回归轮次 53：GUI 告警修复后第三次完整 CTest

### 范围

第三次连续执行相同 46 个 CTest，复核受影响目标和全局状态在重复运行后的稳定性。

### 结果

- 46/46 通过，0 失败，通过率 100%，总实际用时 3.38 秒。
- `TestScrollBarInterplay` 第三次通过，用时 0.37 秒。
- 三轮合计 138 个测试实例，失败、崩溃、超时、Qt plugin 错误和 Debug Error 均为 0。

### 证据

- 脱敏全量与三轮汇总：证据 `E-R53-POST-GUI-FIX-CTEST-THIRD`。

### 判定

通过。告警修复后的全目标构建、定向三轮和全量 CTest 三轮均完成；可合入隔离 GUI 分支并继续
Computer Use `GUI-G01`。

## 回归轮次 54：隔离 GUI 分支合入告警修复并重建

### 范围

把可选 scene item 清理修复、保护测试和 R48～R53 追加记录合入隔离 GUI 分支；保持 Modifier
输入桥编译开关开启、受控推理延迟关闭，重新构建应用。

### 结果

- 合入无冲突，输入桥编译选项仍为 enabled。
- 应用重新编译受影响的 `TimeGraphicsScene`，链接和 Qt 部署成功。
- 未启动受控推理延迟，也未改变其他生产或测试配置。

### 证据

- 脱敏合入与重建摘要：证据 `E-R54-GUI-BRANCH-SCENE-FIX-REBUILD`。

### 判定

通过。进入 Computer Use 单音符双保险，确认实际 GUI 路径不再产生旧 warning。

## 回归轮次 55：空 scene item 告警修复的 Computer Use 双保险

### 范围

启动重建后的隔离 GUI 实例，精确绑定唯一空工程窗口；显式清除 Modifier，切换音符铅笔并绘制
一个与 R48 相同类型的短音符，随后按动作时间边界交叉检查运行日志。

### 结果

- 一枚 C4 附近、约半拍、默认歌词“啦”的音符稳定出现，轨道唱段内同步出现一条缩略标记；
  2 秒后位置和尺寸保持不变。
- 无红色失败条、空红残影、闪烁、弹窗或其他可见异常。
- 对应日志只有一次 edit session 和一次成功 note draw；旧
  `QGraphicsScene::removeItem: cannot remove 0-item` warning 为 0 条。
- 证据采集后终止精确的隔离测试进程，仅丢弃本轮创建的单个未保存测试音符；未写入文件。

### 证据

- 脱敏 Computer Use 与日志交叉摘要：证据 `E-R55-SCENE-WARNING-COMPUTER-USE-GREEN`。

### 判定

通过。自动化保护、全量 CTest 和真实 GUI 提交三层结果一致；可启动干净实例进入 `GUI-G01`。

## 回归轮次 56：GUI-G01 双副本文档打开与相对音频失败

### 范围

在同一隔离实例中依次打开两份内容相同、名称不同的已资格化工程副本；每份工程旁均放置内容
一致的相对音频资源。通过 Computer Use 复核文档替换、轨道与唱段结构、编辑器内容、歌手、
时间线标记和音频波形，并按两次打开的动作边界交叉检查运行日志。

### 结果

- 两份工程均成功整体打开，标题依次切换为 `base-first` 和 `base-second`，无意外未保存标记、
  权限提示、Debug Error、断言或产品错误弹窗。
- 两份工程均稳定显示 5 轨，顺序为英语、日语、和声1、和声2和伴奏音频；前四轨各有歌声
  clip，第五轨为音频 clip。首个歌声 clip 自动激活。
- 歌声编辑器可见音符、歌词、音素边界与标签、音高线和合成波形；四条歌声轨均解析为
  Qixuan，clip 显示跟随轨道歌手，语言为普通话。
- `103.02 → 120` 曲速、`4/4 → 3/4` 拍号、蓝色循环范围和启用的 loop 按钮在两份副本中
  均一致。
- 阻断项：两次打开都由 Standard 与 WavPack 格式入口报告无法打开相对音频文件。已只读确认
  音频与 DSPX 位于同一受控目录、工程引用相对文件名且内容摘要匹配资格基线；因此界面的淡蓝
  着色不能算作有效解码波形或可播放音频，`GUI-G01` 不通过。
- 第二次打开的缓存推理约 6 秒完成，采样期间未见红色失败条、闪烁或弹窗；本轮只记录现象，
  不替代后续推理专项结论。旧空 scene item warning 为 0 条。

### 证据

- 脱敏 Computer Use、双副本结构和相对音频失败摘要：证据
  `E-R56-GUI-G01-RELATIVE-AUDIO-FAIL`。

### 判定

失败（产品阻断）。暂停 `GUI-G02`，先修复 DSPX 相对音频资源的工程目录解析，增加自动化保护，
完成定向测试、全量 CTest 和隔离 GUI 重建后重新执行 `GUI-G01`；本轮永久保留。

## 回归轮次 57：相对音频根因与初始保护

### 范围

复现 `GUI-G01` 相对音频失败，追踪 relative resolver、文档 revision、derived writeback 与
`AudioContext` 媒体探测的先后关系，并为修复加入首批自动化保护。

### 结果

- relative resolver 捕获旧 revision；随后推理推进文档 revision，导致 derived writeback 被保护门
  拒绝。
- `AudioContext` 在工程 source 基准可用前过早执行媒体探测，构成相对音频失败的另一时序根因。
- 初版 `TestAudioAssetResolution` 完成 13 个场景、73 条断言，全部通过。

### 证据

- 根因、初始保护与定向结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R57 小节。

### 判定

通过（根因与初始定向保护）。仍需与异步文件域、Core 和 Architecture 组合复测。

## 回归轮次 58：定向组合发现旧测试预期不一致

### 范围

把初版相对音频修复与 Audio、异步文件域组合执行，检查成功 decode 后的媒体状态语义。

### 结果

- Audio 目标通过。
- `TestAutomationAsyncFileDomains` 出现 1 个旧预期失败：新语义在成功 decode 后把 `Missing`
  规范为 `Normal`，旧测试仍期待 `Missing`。
- 复核确认测试规格已过期，产品修复方向正确；本轮失败永久保留，不以随后成功结果隐藏。

### 证据

- 定向组合与旧预期失败：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R58 小节。

### 判定

失败（测试预期过期，非产品修复回退）。修正预期后必须以新轮次完整重跑组合目标。

## 回归轮次 59：修正预期后的定向组合回归

### 范围

按成功 decode 后状态规范修正旧测试预期，重新执行 Audio、Async、Core 与 Architecture。

### 结果

- Audio 为 13 个场景、73 条断言，全部通过。
- Async 为 34 个场景、141 条断言，全部通过。
- Core 与 Architecture 均通过，组合结果为 0 失败。

### 证据

- 修正预期后的完整定向结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R59 小节。

### 判定

通过。轮次 58 的旧测试规格问题已关闭，继续覆盖 source generation 与 `Unconfirmed`。

## 回归轮次 60：source generation 与 Unconfirmed 增量加固

### 范围

扩展相对音频保护，覆盖 source generation 和 `Unconfirmed` 状态，并在补充断言前后分别执行
完整 Audio 定向目标。

### 结果

- 第一次增量执行为 14 个场景、77 条断言，全部通过。
- 补充断言后的第二次增量执行为 14 个场景、78 条断言，全部通过。
- 两次结果均独立记录，未用第二次结果覆盖第一次。

### 证据

- 两次增量执行明细：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R60 小节。

### 判定

通过。扩展状态保护成立，进入第一版 `full all target` 构建。

## 回归轮次 61：第一版修复全目标构建

### 范围

在第一版相对音频修复与自动化保护上执行 Debug build 的 `full all target` 构建。

### 结果

- `full all target` 构建通过，未出现编译或链接错误。

### 证据

- 全目标构建结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R61 小节。

### 判定

通过。允许在同一 Debug build 上开始第一版完整 CTest 三轮验证。

## 回归轮次 62：第一版首次完整 CTest

### 范围

在轮次 61 的 Debug build 上首次执行全部 47 个已注册 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.72 秒。
- 本批完成后仍继续加固，因此本轮不是最终门禁。

### 证据

- 首次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R62 小节。

### 判定

通过（第一版单轮验证）。继续第二轮，不把本轮扩大为最终稳定性结论。

## 回归轮次 63：第一版第二次完整 CTest

### 范围

不改变第一版 source alias 与 Debug build，第二次执行相同 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.72 秒。
- 本批完成后仍继续加固，因此本轮不是最终门禁。

### 证据

- 第二次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R63 小节。

### 判定

通过（第一版重复验证）。继续第三轮，不替代后续加固版门禁。

## 回归轮次 64：第一版第三次完整 CTest

### 范围

在相同第一版 source alias 与 Debug build 上第三次执行全部 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.65 秒。
- 第一版三轮均通过，但该批之后仍继续加固，不作为最终门禁。

### 证据

- 第三次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R64 小节。

### 判定

通过（第一版三轮完成）。继续 Busy、status 与 source 顺序加固。

## 回归轮次 65：Busy、status 与 source 顺序加固定向回归

### 范围

加固 Busy、status 与 source 的更新顺序；连续执行 Audio，并连续执行
Core、Architecture、Async 组合，检查重复运行稳定性。

### 结果

- Audio 连续 3 次均为 17 个场景、102 条断言、0 失败。
- Core、Architecture、Async 组合连续 3 次全部通过；Async 每次均为 34 个场景、141 条断言、
  0 失败。
- 所有定向执行均未出现失败。

### 证据

- 顺序加固与连续定向结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R65 小节。

### 判定

通过。顺序加固的定向稳定性成立，进入加固版 `full all target` 构建。

## 回归轮次 66：加固版全目标构建

### 范围

在 Busy、status 与 source 顺序加固后的 Debug build 上执行 `full all target` 构建。

### 结果

- `full all target` 构建通过，未出现编译或链接错误。

### 证据

- 加固版全目标构建结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R66 小节。

### 判定

通过。进入加固版三轮完整 CTest。

## 回归轮次 67：加固版首次完整 CTest

### 范围

在轮次 66 的 Debug build 上首次执行全部 47 个已注册 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.97 秒。

### 证据

- 加固版首次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R67 小节。

### 判定

通过。继续第二轮完整复跑。

## 回归轮次 68：加固版第二次完整 CTest

### 范围

不改变加固版 source alias 与 Debug build，第二次执行相同 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.80 秒。

### 证据

- 加固版第二次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R68 小节。

### 判定

通过。继续第三轮完整复跑。

## 回归轮次 69：加固版第三次完整 CTest

### 范围

在相同加固版 source alias 与 Debug build 上第三次执行全部 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.72 秒。

### 证据

- 加固版第三次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R69 小节。

### 判定

通过。加固版三轮完整 CTest 完成；继续补强 TrackController 导入顺序。

## 回归轮次 70：TrackController 导入与 Busy 延期加固

### 范围

补充 `TrackController` 导入预处理与 hash 完成后的 Busy 延期处理，执行定向构建，并连续三轮
复测 Audio、Async、Core 与 Architecture。

### 结果

- 定向构建通过。
- 连续 3 轮中，Audio 每次均为 17 个场景、102 条断言；Async 每次均为 34 个场景、
  141 条断言；Core 与 Architecture 也均通过，全部 0 失败。
- 架构守卫要求 5 类音频完成 handler 先延期、后 remove，防止提前移除破坏 Busy 时序。

### 证据

- TrackController 加固、守卫与三轮定向结果：证据
  `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R70 小节。

### 判定

通过。导入预处理和 Busy 延期顺序已受定向行为与架构守卫共同保护。

## 回归轮次 71：TrackController 加固后全目标构建

### 范围

在 TrackController 加固后的 Debug build 上执行 `full all target` 构建。

### 结果

- `full all target` 构建通过，未出现编译或链接错误。

### 证据

- TrackController 加固后全目标构建：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R71 小节。

### 判定

通过。进入该加固版的三轮完整 CTest。

## 回归轮次 72：TrackController 加固后首次完整 CTest

### 范围

在轮次 71 的 Debug build 上首次执行全部 47 个已注册 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.74 秒。

### 证据

- 首次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R72 小节。

### 判定

通过。继续第二轮完整复跑。

## 回归轮次 73：TrackController 加固后第二次完整 CTest

### 范围

不改变 source alias 与 Debug build，第二次执行相同 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.79 秒。

### 证据

- 第二次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R73 小节。

### 判定

通过。继续第三轮完整复跑。

## 回归轮次 74：TrackController 加固后第三次完整 CTest

### 范围

在相同 source alias 与 Debug build 上第三次执行全部 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.77 秒。

### 证据

- 第三次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R74 小节。

### 判定

通过。TrackController 加固后三轮完整 CTest 完成；统一格式化后再做完整重编译。

## 回归轮次 75：统一格式化后的完整重编译

### 范围

统一格式化全部修改后，先完整重编译 Debug 应用，再执行 Debug build 的 `full all target` 编译。

### 结果

- Debug 应用完整重编译 132 步通过。
- 随后的 `full all target` 编译 64 步通过，0 错误。

### 证据

- 格式化后两阶段构建结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R75 小节。

### 判定

通过。最终格式源码和完整 Debug build 已就绪，进入最终三轮完整 CTest。

## 回归轮次 76：最终源码首次完整 CTest

### 范围

在轮次 75 的最终 source alias 与 Debug build 上首次执行全部 47 个已注册 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.97 秒。

### 证据

- 最终源码首次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R76 小节。

### 判定

通过。继续相同源码和二进制的第二轮完整复跑。

## 回归轮次 77：最终源码第二次完整 CTest

### 范围

不改变最终 source alias、Debug build 或测试环境，第二次执行相同 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.77 秒。

### 证据

- 最终源码第二次完整结果：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R77 小节。

### 判定

通过。继续第三轮完整复跑以关闭最终自动化稳定性门禁。

## 回归轮次 78：最终源码第三次完整 CTest

### 范围

在相同最终 source alias 与 Debug build 上第三次执行全部 47 个 CTest，并汇总三轮进程终态。

### 结果

- 47/47 通过，0 失败，总实际用时 2.76 秒。
- 三轮合计执行 141 个测试实例；失败、崩溃、超时、Qt plugin 错误和 Debug Error 均为 0。

### 证据

- 最终源码第三次结果与三轮汇总：证据 `E-R57-R78-AUDIO-RECOVERY-AUTOMATED` 的 R78 小节。

### 判定

通过。相对音频修复已完成根因诊断、失败留档、定向加固、`full all target` 构建与最终三轮
完整 CTest 自动化门禁；`GUI-G01` 的外部观察仍须以新轮次单独复测。

## 回归轮次 79：相对音频纠错 Computer Use 传输阻塞

### 范围

在执行任何产品动作前建立 Computer Use 连接，准备复测 `GUI-G01` 的相对音频纠错；由子任务
和主会话分别按恢复规则执行只读连接检查。

### 结果

- 子任务首次 `list_apps` 即报告 `Transport closed`；按恢复规则执行 reset 后，传输仍为 closed。
- 主会话独立执行 init/list、等待后的轻量重试、reset、重新 init/list；各步均报告
  `Transport closed`，且 reset 本身也未到达传输端。
- `DsEditorLite` 进程为 0；未启动应用、未绑定窗口、未向 Windows 发送输入，产品动作与产品
  断言均未执行。

### 证据

- 脱敏传输恢复与产品边界摘要：证据 `E-R79-COMPUTER-USE-TRANSPORT-BLOCKED`。

### 判定

阻塞（Computer Use 传输基础设施，不计产品失败）。本轮永久保留；传输恢复后以新轮次执行
相对音频纠错的 GUI 外部观察，不用后续结果覆盖本轮。

## 回归轮次 80：隔离 GUI 分支合入音频修复并全目标构建

### 范围

把正式相对音频修复合入隔离 GUI 分支，在该分支的 Debug build 上执行 `full all target` 构建，
确认应用和音频解析保护目标共同进入构建图。

### 结果

- `full all target` 共完成 374 个构建步骤并通过，编译错误和链接错误均为 0。
- `TestAudioAssetResolution` 已包含在成功构建的测试目标中。

### 证据

- 合入后全目标构建摘要：证据 `E-R80-R84-GUI-BRANCH-AUTOMATED-GATE` 的 R80 小节。

### 判定

通过（隔离 GUI 分支构建门禁）。继续在同一 Debug build 上执行完整 CTest。

## 回归轮次 81：首次完整 CTest 调度环境错误

### 范围

首次从 Git Bash 直接调用 CTest，准备发现并执行轮次 80 构建的全部测试目标。

### 结果

- shell 报告 `ctest: command not found`，退出码为 127。
- 失败发生在测试发现前，启动的测试数为 0；没有执行任何产品或测试断言。
- 本轮失败永久保留，不以随后成功结果隐藏。
- 随后从该 Debug build 的 `CMakeCache` 解析已配置的 CTest 可执行文件，没有硬编码工具
  安装位置。

### 证据

- 调度失败与解析恢复摘要：证据 `E-R80-R84-GUI-BRANCH-AUTOMATED-GATE` 的 R81 小节。

### 判定

失败（测试调度环境错误，非产品失败）。使用配置缓存解析出的工具，以新轮次重新开始完整
CTest 门禁。

## 回归轮次 82：解析 CTest 工具后的首次完整执行

### 范围

使用从 `CMakeCache` 解析的 CTest 可执行文件，在轮次 80 的 source alias 与 Debug build 上
首次执行全部 47 个已注册测试。

### 结果

- 47/47 通过，0 失败，总实际用时 4.34 秒。

### 证据

- 工具解析后的首次完整结果：证据 `E-R80-R84-GUI-BRANCH-AUTOMATED-GATE` 的 R82 小节。

### 判定

通过。继续相同源码和二进制的第二轮完整复跑。

## 回归轮次 83：隔离 GUI 分支第二次完整 CTest

### 范围

不改变 source alias、Debug build 或测试环境，第二次执行相同 47 个 CTest。

### 结果

- 47/47 通过，0 失败，总实际用时 2.79 秒。

### 证据

- 第二次完整结果：证据 `E-R80-R84-GUI-BRANCH-AUTOMATED-GATE` 的 R83 小节。

### 判定

通过。继续第三轮完整复跑以关闭隔离 GUI 分支自动化稳定性门禁。

## 回归轮次 84：隔离 GUI 分支第三次完整 CTest

### 范围

在相同 source alias 与 Debug build 上第三次执行全部 47 个 CTest，并汇总三轮进程终态。

### 结果

- 47/47 通过，0 失败，总实际用时 2.78 秒。
- 三轮合计执行 141 个测试实例；失败、崩溃、超时、Qt plugin 错误和 Debug Error 均为 0。

### 证据

- 第三次完整结果与三轮汇总：证据 `E-R80-R84-GUI-BRANCH-AUTOMATED-GATE` 的 R84 小节。

### 判定

通过。正式相对音频修复合入隔离 GUI 分支后的 `full all target` 构建与三轮完整 CTest 门禁
完成；轮次 81 的调度环境失败继续永久保留。

## 回归轮次 85：Computer Use 新会话传输阻塞

### 范围

以新会话建立 Computer Use 连接，准备继续相对音频纠错的 GUI 外部观察；连接失败后严格只
执行一次 reset 恢复检查。

### 结果

- 新会话 init 即报告 `Transport closed`。
- 严格执行一次 reset 后，传输仍为 closed；没有继续追加恢复尝试。
- 未枚举或绑定窗口，未启动进程，未向 Windows 发送输入，也未执行清理或关闭动作。
- 没有触及产品状态或执行产品断言。

### 证据

- 脱敏传输恢复与未操作边界摘要：证据 `E-R85-COMPUTER-USE-TRANSPORT-BLOCKED`。

### 判定

阻塞（Computer Use 传输基础设施，非产品失败）。本轮永久保留；下一次连接尝试必须使用新的
回归轮次，不用后续结果覆盖本轮。

## 回归轮次 86：Computer Use 主会话再次传输阻塞

### 范围

在主会话中再次建立 Computer Use 连接，准备执行相对音频纠错的 GUI 外部观察；按恢复约束
执行一次轻量重试、一次 reset 和一次最终重连检查。

### 结果

- 初次 init/list、轻量重试、reset 以及最终 init/list 均报告 `Transport closed`。
- 未枚举或绑定窗口，未启动进程，未向 Windows 发送输入，也未执行清理或关闭动作。
- 没有触及产品状态或执行产品断言。

### 证据

- 脱敏传输恢复与未操作边界摘要：证据 `E-R86-COMPUTER-USE-TRANSPORT-BLOCKED`。

### 判定

阻塞（Computer Use 传输基础设施，非产品失败）。本轮永久保留；继续进行不依赖 Computer Use
的确定性覆盖，后续连接恢复检查另开新轮次。

## 回归轮次 87：编辑维度矩阵首批执行

### 范围

首次运行编辑域逐 operation 维度矩阵的 15 项可执行批次。

### 结果

- 65 个场景、194 条断言；64 个场景通过，2 条断言失败。
- 失败来自测试把空 Singer/Speaker 选择误判为非法；真实 handler 允许该输入切换 owned
  context，不是产品缺陷。

### 证据

- 首批原始结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R87 小节。

### 判定

失败（测试规格假设错误，非产品失败）。保留本轮并更正测试规格。

## 回归轮次 88：编辑维度矩阵 40 项首次全集

### 范围

把编辑矩阵扩展到全部 40 项 operation，并执行初次全集。

### 结果

- 274 个场景、824 条断言；269 个场景通过，5 个场景失败、6 条断言失败。
- 失败同时包含 `notes.quantize` 非法网格、Note 请求指纹和两项 split 测试假设。

### 证据

- 全集首次结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R88 小节。

### 判定

失败。先消除测试假设，再冻结生产失败集合。

## 回归轮次 89：编辑维度矩阵测试假设纠正

### 范围

纠正 split created/affected 顺序及重放请求构造后，完整复跑 40 项矩阵。

### 结果

- 274 个场景、824 条断言；271 个场景通过，4 条断言失败。
- split 场景已通过；剩余均指向非法 quantize 和 Note 请求指纹。

### 证据

- 纠正后结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R89 小节。

### 判定

失败（稳定生产行为缺陷）。继续收窄同键异参的指纹变体。

## 回归轮次 90：编辑维度矩阵冻结失败集

### 范围

以只改变 Note ID/列表而保持其他参数不变的冲突请求，冻结编辑域最终失败集合。

### 结果

- 40/40 operation、273 个场景、825 条断言；266 个场景通过，7 个场景失败、8 条断言失败。
- `quantize=0` 被错误接受并修改文档。
- `notes.move/remove/resize_left/resize_right/quantize/set_word_properties` 的请求集合在同一函数
  调用中既参与 fingerprint 又被 move-capture，受未指定实参求值顺序影响而产生空指纹。

### 证据

- 冻结失败结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R90 小节。

### 判定

失败（生产缺陷已决定性复现）。进入生产修复。

## 回归轮次 91：编辑指纹与量化修复验证

### 范围

修复六个 Note 命令的指纹求值顺序，并在换算前校验 quantize 后完整复跑。

### 结果

- 40/40 operation、273/273 个场景通过、825 条断言、0 失败。
- 精确 operation 集合与每项 6–7 场景门禁均通过。

### 证据

- 修复后结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R91 小节。

### 判定

通过。进入 CMake 正式接入验证。

## 回归轮次 92：编辑维度矩阵正式 CTest

### 范围

通过标准 Debug preset 构建并注册 `TestAutomationEditingDimensions`，执行其 CTest。

### 结果

- 1/1 通过，0 失败。

### 证据

- 正式接入结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R92 小节。

### 判定

通过。继续与原编辑域回归联合执行。

## 回归轮次 93：新旧编辑域联合回归

### 范围

联合执行 `TestAutomationEditingDomains` 与 `TestAutomationEditingDimensions`。

### 结果

- 2/2 通过，0 失败。

### 证据

- 联合结果：`E-R87-R93-EDITING-DIMENSIONS` 的 R93 小节。

### 判定

通过。编辑域 40 项逐 operation 维度门禁关闭。

## 回归轮次 94：异步维度矩阵首批执行

### 范围

首次运行提取、音频导出和任务查询/取消的 5 项异步矩阵。

### 结果

- 5 项 operation、36 个场景、49 条断言、0 失败。

### 证据

- 首批结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R94 小节。

### 判定

通过。继续扩展推理操作。

## 回归轮次 95：异步维度矩阵加入推理域

### 范围

加入 12 项推理写回 operation，复跑当前完整矩阵。

### 结果

- 17 项 operation、108 个场景、121 条断言、0 失败。

### 证据

- 扩展结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R95 小节。

### 判定

通过。继续扩展音频资产写回。

## 回归轮次 96：异步维度矩阵加入音频资产域

### 范围

加入 6 项 audio clip 解析、缓存、状态、哈希和重定位 operation。

### 结果

- 23 项 operation、156 个场景、169 条断言、0 失败。

### 证据

- 音频资产扩展结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R96 小节。

### 判定

通过。继续加入格式查询。

## 回归轮次 97：异步维度矩阵加入格式查询

### 范围

加入格式枚举及宿主错误维度并完整复跑。

### 结果

- 24 项 operation、165 个场景、186 条断言、0 失败。

### 证据

- 格式扩展结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R97 小节。

### 判定

通过。继续加入文件和文档操作。

## 回归轮次 98：异步维度矩阵加入导出与文档操作

### 范围

加入 MIDI 导出、音频预览/清理和部分文档生命周期维度。

### 结果

- 28 项 operation、193 个场景、214 条断言、0 失败。

### 证据

- 文件域扩展结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R98 小节。

### 判定

通过。继续完成 34 项全集。

## 回归轮次 99：异步维度矩阵 34 项全集

### 范围

完成异步/文件域 34 项 operation 全集，并启用精确集合、场景 ID、6–9 场景及适用状态门禁。

### 结果

- 34/34 operation、233 个场景、254 条断言、0 失败。
- Queued 11、Running 19、CancelRequested 12、Committing 7、terminal 26；四个适用异步
  operation 的逐项五阶段门禁均通过。

### 证据

- 全集结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R99 小节。

### 判定

通过。没有发现新的生产缺陷。

## 回归轮次 100：异步维度矩阵正式重建执行

### 范围

正式接入测试树后重建可执行文件，直接执行包含最终 stdout 门禁摘要的 34 项矩阵。

### 结果

- 34/34 operation、233 个场景、254 条断言、0 失败；每项 6–9 场景。

### 证据

- 正式重建结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R100 小节。

### 判定

通过。继续正式 CTest。

## 回归轮次 101：异步维度矩阵正式 CTest

### 范围

执行已注册 `TestAutomationAsyncDimensions` 的 CTest。

### 结果

- 1/1 通过，0 失败，实际用时 1.51 秒。

### 证据

- CTest 结果：`E-R94-R101-ASYNC-DIMENSIONS` 的 R101 小节。

### 判定

通过。异步/文件域 34 项逐 operation 门禁关闭。

## 回归轮次 102：运行时维度矩阵首批执行

### 范围

首次运行 application 与 playback 的 12 项逐 operation 矩阵。

### 结果

- 12 项 operation、93 个场景、126 条断言、0 失败；每项 6–9 场景。

### 证据

- 首批结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R102 小节。

### 判定

通过。继续加入 Editor。

## 回归轮次 103：运行时维度矩阵加入 Editor

### 范围

扩展到 application、playback、editor 共 27 项 operation。

### 结果

- 212 个场景、292 条断言、11 失败。
- 10 个 Editor operation 的非法输入在进入 Dispatcher 前返回，导致稳定错误缺少
  `operationId`；`set_quantize` 含两条失败输入。

### 证据

- Editor 扩展结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R103 小节。

### 判定

失败（生产契约缺陷）。继续扩全集以检查同类路径。

## 回归轮次 104：运行时维度矩阵 48 项首次全集

### 范围

加入 settings、Recent、搜索路径、packages 和 presets，首次执行 48 项全集。

### 结果

- 357 个场景、495 条断言、17 失败。
- 除 Editor 外，Recent add/remove 与 preset delete 存在同类错误装饰缺失；preset save 还暴露
  未知非空 ID 被新建和权重未归一化。

### 证据

- 全集首次结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R104 小节。

### 判定

失败（生产缺陷）。格式化后复跑确认稳定性。

## 回归轮次 105：运行时维度矩阵格式化后复跑

### 范围

不改变测试语义，在格式化后重新编译并完整执行 48 项矩阵。

### 结果

- 48 项、357 个场景、495 条断言、17 失败；失败集合与轮次 104 完全一致。

### 证据

- 稳定复跑结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R105 小节。

### 判定

失败（稳定生产缺陷）。继续加入逐项适用维度机器门禁。

## 回归轮次 106：运行时适用维度门禁首次执行

### 范围

加入 8 类适用维度及逐 operation mask 检查后完整运行。

### 结果

- 48 项、357 个场景、543 条断言、19 失败。
- 其中 17 条为既有生产失败，2 条为测试分类器遗漏；operation 集合与场景数门禁通过。

### 证据

- 维度门禁首次结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R106 小节。

### 判定

失败（含 17 条生产失败与 2 条测试矩阵失败）。仅修正分类器后复跑。

## 回归轮次 107：共享构建重叠诊断

### 范围

尝试重编独立运行时矩阵时，与主任务刷新共享 Debug 构建树重叠。

### 结果

- 编译阶段因生成 include 暂时不可见而停止；未进入执行，运行场景为 0。
- 没有形成产品通过或失败判定；随后停止并等待共享构建稳定。

### 证据

- 构建重叠诊断：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R107 小节。

### 判定

阻塞（共享构建调度冲突，非产品失败）。保留本轮并以新轮次重试。

## 回归轮次 108：运行时分类器修正后全集

### 范围

共享构建稳定后，仅修正测试维度分类器并完整复跑。

### 结果

- 48/48 operation、357 个场景、543 条断言、17 失败。
- 精确集合、每项 6–9 场景和适用维度 mask 全部通过；17 条均为生产行为失败。

### 证据

- 分类器修正结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R108 小节。

### 判定

失败（生产缺陷冻结）。继续重复一次确认无 flaky。

## 回归轮次 109：运行时生产失败稳定复跑

### 范围

以相同源码、二进制和环境再次执行冻结的 48 项矩阵。

### 结果

- 48 项、357 个场景、543 条断言、17 失败；与轮次 108 完全一致。

### 证据

- 稳定复跑结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R109 小节。

### 判定

失败（稳定生产缺陷）。进入 Facade 修复。

## 回归轮次 110：运行时契约首轮修复验证

### 范围

把非法输入移入 Dispatcher handler，收紧 preset 更新 ID，并加入权重归一化后完整复跑。

### 结果

- 48 项、357 个场景、543 条断言；542 条通过、1 条失败。
- 错误装饰和未知 ID 已全部通过；剩余失败来自测试把预设权重误按 N 个完整权重表示。
- 静态复核确认既有持久化与 SpeakerMix 模型使用 N−1 个显式权重。

### 证据

- 首轮修复结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R110 小节。

### 判定

失败（测试表示假设错误，同时发现 Facade 原尺寸校验与模型不一致）。保留本轮并复用既有模型
归一化语义修正。

## 回归轮次 111：运行时契约最终修复验证

### 范围

将 Facade 与测试统一到 N−1 显式权重契约，复用模型归一化函数后完整执行。

### 结果

- 48/48 operation、357/357 个场景、543 条断言、0 失败。
- operation 精确集合、每项 6–9 场景及全部适用维度 mask 通过。

### 证据

- 最终修复结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R111 小节。

### 判定

通过。进入正式 CTest。

## 回归轮次 112：运行时维度矩阵正式 CTest

### 范围

标准 Debug preset 重建并执行已注册 `TestAutomationRuntimeDimensions`。

### 结果

- 1/1 通过，0 失败，实际用时 1.77 秒。

### 证据

- 正式 CTest：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R112 小节。

### 判定

通过。继续旧核心/运行时回归。

## 回归轮次 113：旧核心与运行时域联合回归

### 范围

强制重建并联合执行 `TestAutomationCore` 与 `TestAutomationRuntimeDomains`。

### 结果

- 2/2 通过，0 失败，总实际用时 0.16 秒。

### 证据

- 联合结果：`E-R102-R113-RUNTIME-DIMENSIONS` 的 R113 小节。

### 判定

通过。运行时 48 项逐 operation 门禁关闭。

## 回归轮次 114：最终 Debug 全目标构建

### 范围

在编辑、异步和运行时三套逐 operation 矩阵及生产修复提交后，执行 Debug `all` 目标构建。

### 结果

- 78 个构建步骤全部通过；应用、AutomationTestSupport 与 50 个测试可执行文件均成功编译、
  链接。
- 编译和链接错误为 0。

### 证据

- 全目标构建摘要：`E-R114-R117-FINAL-AUTOMATED-GATE` 的 R114 小节。

### 判定

通过。进入三轮完整 CTest。

## 回归轮次 115：50 项首次完整 CTest

### 范围

串行执行 Debug build 中全部 50 个已注册 CTest；单测试超时为 60 秒。

### 结果

- 50/50 通过，0 失败，总实际用时 8.25 秒。
- 无超时、Qt platform plugin 错误或 Debug Error。

### 证据

- 首轮完整结果：`E-R114-R117-FINAL-AUTOMATED-GATE` 的 R115 小节。

### 判定

通过。继续相同源码与二进制的第二轮完整复跑。

## 回归轮次 116：50 项第二次完整 CTest

### 范围

不改变源码、构建或测试环境，第二次串行执行相同 50 个 CTest。

### 结果

- 50/50 通过，0 失败，总实际用时 8.13 秒。
- 无超时、Qt platform plugin 错误或 Debug Error。

### 证据

- 第二轮完整结果：`E-R114-R117-FINAL-AUTOMATED-GATE` 的 R116 小节。

### 判定

通过。继续第三轮完整复跑。

## 回归轮次 117：50 项第三次完整 CTest

### 范围

在相同源码和 Debug build 上第三次执行全部 50 个 CTest，并汇总逐 operation 维度覆盖。

### 结果

- 50/50 通过，0 失败，总实际用时 8.11 秒。
- 三轮合计 150 个测试实例，失败、崩溃、超时、Qt plugin 错误和 Debug Error 均为 0。
- 三套直接维度矩阵覆盖 Catalog 122/122 operation：编辑 40 项/273 场景/825 断言，异步
  34 项/233 场景/254 断言，运行时 48 项/357 场景/543 断言；合计 863 场景、1,622 断言。

### 证据

- 第三轮完整结果与汇总：`E-R114-R117-FINAL-AUTOMATED-GATE` 的 R117 小节。

### 判定

通过。全量确定性自动化门禁关闭；恢复 Computer Use 时如再次遭遇基础设施阻塞，将立即停止
并等待用户重启 Codex。

## 回归轮次 118：隔离 GUI 分支最终重建与启动资格

### 范围

将最终生产提交合入隔离 GUI 分支后，按项目 CMake Build 规范重新构建 `DsEditorLite`，随后启动
桥 test mode 实例并检查 Computer Use 前置环境。

### 结果

- GUI 分支提交短标识为 `a50e75be`；53 个构建步骤全部成功。
- 可执行文件 SHA-256 为
  `745cb7b2394f8379e8b7d56abea91057a61f6fc163b2179e77784162baaa535f`。
- 测试实例 PID 为 `17856`，WindowId 为 `41291992`。
- 桥 test mode 实例正常；未出现权限提示、Debug Error 或意外模态窗口。

### 证据

- 脱敏重建、进程与窗口资格摘要：`E-R118-R121-GUI-AUDIO-RECOVERY` 的 R118 小节。

### 判定

通过。隔离 GUI 实例具备继续 Computer Use 回归的资格。

## 回归轮次 119：文件对话框输入位置误操作

### 范围

首次尝试打开 DSPX 工程时，Computer Use 误将完整工程路径输入 Windows 文件对话框地址栏，
而非文件名输入框。

### 结果

- Windows 弹出“选择应用打开”系统模态窗口。
- 使用 Esc 关闭该模态窗口后，产品文档与编辑状态保持不变。
- 本轮没有执行到产品的工程打开流程。

### 证据

- 脱敏操作过程摘要：`E-R118-R121-GUI-AUDIO-RECOVERY` 的 R119 小节。

### 判定

测试过程错误，不计为产品失败；保留本轮记录并在下一轮校正输入位置。

## 回归轮次 120：校正 GUI-G01 双副本打开与内容检查

### 范围

在文件名输入框中分别选择 `FIRST_COPY` 与 `SECOND_COPY`，验证两份工程副本及其物理独立的
伴随音频文件能够完整加载，并检查工程结构、编辑内容与运行状态。

### 结果

- 两次打开后的窗口标题分别为 `base-first` 与 `base-second`。
- 两份工程均显示 5 条轨道：英语、日语、和声1、和声2及伴奏音频；前 4 条为歌声轨，最后
  1 条为音频轨。
- 两份工程的音频轨均显示清晰、非空的音频波形。
- 首条歌声 Clip 的音符、歌词、音素、音高线和合成波形保持稳定，无闪烁或缺失。
- Tempo 从 103.02 调整为 120，拍号从 4/4 调整为 3/4，Loop 状态可见。
- 未出现未保存标记、错误提示或意外模态窗口。

### 证据

- 脱敏双副本 Computer Use 摘要：`E-R118-R121-GUI-AUDIO-RECOVERY` 的 R120 小节。

### 判定

通过。GUI-G01 双副本工程及物理独立伴随音频加载正常。

## 回归轮次 121：相对音频路径解析与解码写回

### 范围

打开 `RELATIVE_AUDIO_PROJECT`，等待 8 秒并检查相对路径音频的解析、实际解码和界面写回。

### 结果

- 等待期间未出现确认、错误或其他意外模态窗口；窗口标题为 `relative-audio`。
- 工程轨道结构与双副本一致。
- 滚动至伴奏音频轨后，轨道显示清晰、非空的音频波形。
- 可见波形证明相对路径解析后完成了实际音频解码及界面写回，而非仅完成工程结构加载。

### 证据

- 脱敏相对音频 Computer Use 摘要：`E-R118-R121-GUI-AUDIO-RECOVERY` 的 R121 小节。

### 判定

通过。相对音频路径修复获得 Computer Use 外部观察双保险。

## 回归轮次 122：GUI-G02 文档生命周期与最近工程

### 范围

在来源提交 `a50e75be`、可执行文件 SHA-256
`745cb7b2394f8379e8b7d56abea91057a61f6fc163b2179e77784162baaa535f` 的隔离 GUI 实例上，
通过 Computer Use 验证新建、另存为、保存、最近工程容量与排序、移除、失效项恢复及清空行为。

### 结果

- 新工程增加第二轨并在第二轨创建歌声片段后另存为 `DOC_A`；继续增加第三轨并另存为
  `DOC_B`，再增加第四轨并执行 Ctrl+S。各阶段窗口标题和 dirty 标记均正确。
- 标题弹层和 File 菜单均可打开 `DOC_A`、`DOC_B`，两个文档各自保存的轨道与片段结构均
  正确恢复。
- 依次打开 `RECENT_01` 至 `RECENT_09` 后，标题弹层严格保留最近 5 项。
- 当前项的 more/remove 操作 disabled；非当前项 `RECENT_08` 的 remove 操作 enabled，移除后
  只从列表消失，其物理文件仍存在且非空。
- 再打开 `DOC_A` 后，File → Recent 子菜单严格包含 10 项。
- 将 `RECENT_01` 以可恢复方式移出原位置后点击该项，应用显示文件不存在提示，当前
  `DOC_A` 保持不变，失效项随后自动从 Recent 列表移除。
- 执行 Clear Recent 后，标题弹层与 File → Recent 子菜单均显示无最近工程，清理操作
  disabled；随后 Ctrl+N 成功进入空新工程。
- 文件证据相互一致：`DOC_A` 为 2177 B，摘要 `16e7b2...6042`；`DOC_B` 为 2547 B，摘要
  `a48327...0045`，两者内容不同且 `DOC_B` 修改时间晚于 `DOC_A`。`RECENT_08` 原文件仍为
  2177 B，摘要与 `DOC_A` 相同；`RECENT_01` 原位置已不存在，`RECOVERED_RECENT_01`
  artifact 为 2177 B，摘要与 `DOC_A` 相同。
- 未出现产品错误、Debug Error 或异常模态窗口。

### 证据

- 脱敏文档生命周期与 Recent Computer Use 摘要：`E-R122-GUI-G02-DOCUMENT-RECENT`。

### 判定

通过。GUI-G02 的文档保存分支、Recent 双入口、容量边界、删除语义、失效恢复与清空状态均
符合预期。

## 回归轮次 123：GUI-G03 文档保护与失败保留

### 范围

通过 Computer Use 验证 dirty 文档在 New、Open 和退出流程中的取消、保存与不保存分支，
并验证畸形工程、不支持格式打开失败时保留旧文档。长异步 Busy 分支按能力装备状态单独判定。

### 结果

- 从干净的 2 轨 `DOC_A` 增加一轨后，文档成为 dirty 的 3 轨状态。第一次执行 New 并
  Cancel，dirty 标记、标题、3 轨结构和内容全部保持；第二次执行 New 并选择不保存后，
  进入真正空白的新工程。
- 重新打开 `DOC_A` 并加轨成为 dirty 后，Open 选择 `DOC_B`；第一次在保存警告中 Cancel，
  `DOC_A` 的 dirty 标记、3 轨结构和内容保持。重复 Open 并选择 Save 后，`DOC_A` 成功保存为
  3 轨，随后打开 `DOC_B`；标题为干净状态，结构为 4 轨。
- 打开 `MALFORMED_PROJECT` 时显示“打开工程失败 / Failed to parse project file. errors:
  Error type: 1”，原 `DOC_B` 的标题和内容保持。
- 打开 `UNSUPPORTED_TEXT` 时显示“不支持的文件 / 无法识别的文件格式：txt”，原 `DOC_B`
  同样保持。
- `DOC_B` 加轨成为 dirty 后点击退出，应用显示“要保存更改吗？”。Cancel 后窗口、dirty
  标记和内容均保持。
- 随后 Ctrl+Z 使模型回到保存基线，Undo disabled、Redo enabled，但标题仍为 dirty。该现象
  作为非阻塞诊断记录并留待后续跟踪，不影响本轮文档保护结论；重新打开 `DOC_B` 并选择
  不保存后完成恢复，最终标题干净，Undo/Redo 均 disabled。
- `CAP-LONG-ASYNC` 未装备；现有 `AUDIO_LONG` 的真实解码约 0.07 秒，不能稳定命中 Busy，
  因而条件 Busy 子项判定为 NOT EQUIPPED，不伪造通过或失败结果。
- 匿名文件证明：`DOC_A_SAVED` 为 2362 B、SHA-256
  `230f91a305b1fa4d0a9590eed6a2f7e97ed40028167525c0f7dd51499ae95750`、3 轨；
  `DOC_B_BASELINE` 为 2547 B、SHA-256
  `a48327d961f654b2821569311e588906da175e6e1e0a889253fa04578e490045`、4 轨，且与本轮前一致，
  未被临时编辑污染。`MALFORMED_PROJECT` 为 4096 B、SHA-256
  `4f18066ee9ed1268e3a07ddf1a99b65659026007675640800ec4e00ce02ad3f9`；
  `UNSUPPORTED_TEXT` 为 164 B、SHA-256
  `332a10535f7fa4c2036cba7015f50c3eacda39aed77b641b00f9905e6e598fbc`。
- 两次元素索引失效后改用最新截图坐标继续执行，属于测试驱动适配；本轮无产品错误、
  Debug Error、非预期模态或 Computer Use 基础设施阻塞。

### 证据

- 脱敏文档保护、失败保留与能力判定摘要：`E-R123-GUI-G03-DOCUMENT-PROTECTION`。

### 判定

GUI-G03 核心通过；条件能力 `CAP-LONG-ASYNC` 为 NOT EQUIPPED。Undo 回到保存模型后标题仍
dirty 的非阻塞诊断已保留，后续单独跟踪。

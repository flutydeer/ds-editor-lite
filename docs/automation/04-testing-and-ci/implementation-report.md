# 四期实现报告

## 1. 交付概况

本期完成测试体系重构、重要行为补测、跨平台测试基础设施和 Linux CI。Windows 本地已完成包含真实声库的完整构建与测试；Linux 完整构建、规定测试集合及依赖冷缓存、暖缓存和升级缓存路径均有实际成功证据。验证范围与结果见[测试报告](test-report.md)，各受测版本的执行证据和审查结论见 [PR #187](https://github.com/flutydeer/ds-editor-lite/pull/187)。

本报告记录本期交付设计及验收事实，适用至本期发布。测试数量、执行耗时、依赖具体版本和逐次运行结果由源码及 CI 产物承载，不在报告中维护逐提交快照。

## 2. 测试结构与生产复用

测试按基础数据与算法、编辑与应用状态、文档文件与异步工作流、自动化接口与 Connector、真实应用进程、界面组件与交互六类组织。Qt Test 维护用例、断言、数据行和真实 Qt 输入；CTest 负责标签、环境、超时、互斥及结果汇总。一个测试程序默认对应一个 CTest 注册项，具体用例只在 Qt Test 中维护。跨 Host 进程场景因 GUI 运行条件单独注册，不另建函数清单镜像。

`lite_tests` 聚合测试目标，`tests` build preset 同时构建 Editor、Connector 和全部测试，原先独立的 ICU wrapper 测试也纳入统一入口。大入口按行为拆分，数据驱动用例重建状态；有效回归归入所属功能，纯实验入口移出自动测试集合。迁移去向见[覆盖矩阵](test-coverage-matrix.md)。

新增行为默认加入所属 Qt Test 类的 slot 或数据行，程序边界由组件依赖、共享 fixture 和运行条件决定。游标与协议、颜色与图标、视图与撤销控制器、应用级服务分别合并；输入转换与 Speaker Mix 准入归入同一套件。应用 GUI 共享 AppContext，钢琴窗、轨道、参数曲线和音频导出交互按源文件组织，各自创建和清理需要的控件。每个程序只执行一次 `qExec`，CTest 不维护测试函数清单。

曲线变换、时间线、音频锚点、参数支持、主题、声线混合、缓存、推理配置和歌词规则的旧布尔测试函数迁入直接 Qt Test slot，保留原输入与容差契约。数值比较提供实际值与期望值；确需共享的辅助断言统一保留调用位置。提前失败通过局部所有权或 fixture cleanup 清理预览对象、曲线及环境变量。

生产实现按依赖边界共享：

- `EditorAutomationCore` 承接模型、编辑服务、Facade，以及歌词规则、整片段剪贴板和曲线 JSON 编码等可共享实现；应用与领域测试链接同一目标。
- `EditorInferenceCore` 承接推理任务输入转换、通用输入模型与 Speaker Mix 校验；应用与输入转换测试共用生产目标，测试不再用空方法满足链接，也不需要完整 GUI Runtime。
- `EditorRuntime` 承接主程序之外的应用实现、Qt 资源、翻译和界面依赖；Editor、应用工作流和应用 GUI 测试复用这些实现。
- `AutomationTestSupport` 承接 fixture。外部服务、时序和设备条件使用必要替身，不复制业务逻辑。

整片段与音符复制共用曲线编码，TrackController 与领域测试共用粘贴位置准备逻辑。生产库缺失的 ICU、提取模块依赖补在所属库自身，避免为每个测试追加链接补丁。接口测试移除了工具总数、完整工具清单和 Schema 字段镜像；少量跨层场景比较业务结果、错误和副作用。

## 3. 行为补测与产品修复

产品能力复查与覆盖率采样共同确定补测范围，除整理存量外，补齐了以下重要行为：

- DSPX 编辑内容往返，以及丢弃保存分支后的 dirty 状态。
- 整片段参数复制、跨轨粘贴、歌词拆分规则和推理结果 Apply/Drop/Defer 门控。
- 真实钢琴窗鼠标绘制、已有音符拖动预览与取消、一次提交及撤销恢复；ClipboardController 和 QClipboard 的复制、剪切、粘贴及无效输入。
- 无需声库的短 WAV 导入、真实 Editor 导出任务、输出解码与文档状态检查。
- 离线导出后原先打开或关闭的混音器状态恢复，复用 `TestApplicationWorkflows` 中的 Headless AppContext。
- 轨道声线变化向实际 SingingClip 传播、自有声线隔离及切回继承，替换遮蔽传播行为的空 stub。
- 导出对话框的格式与文件名预览、轨道来源与混音选择，以及取消后重开恢复配置；通过真实键盘和鼠标检查界面、设置及文件副作用。
- 默认参数编辑器与轨道视图的真实鼠标路径：绘制参数、拖动片段，分别验证预览不提前修改工程、提交只产生一个历史步骤、Escape 取消以及撤销后的图形与模型恢复。
- 运行中手动重启推理：暂停真实 duration worker 后替换流水线，检查旧任务释放、替换任务终态和队列清理；该回归无需安装声库。
- 关闭自动推理时的显式完整推理：验证本次手动请求可进入声学阶段，完成后恢复后台等待策略；使用实际声库完成推理、声码器与 WAV 导出闭环。

补测和跨平台执行确认并修复了保存点悬空导致的误判、歌词空匹配重复前缀、公开播放调用被设备错误弹窗阻塞、退出响应发送前连接销毁，以及导出后对原先关闭的混音器调用 `open(0,0)` 等问题。修复保留原有行为断言；具体根因及验证方式见测试报告。

真实声库执行还发现，重建流水线直接销毁旧状态对象，会同时丢失驱动任务队列的完成接线。替换流程改为先取消该片段的任务，让已有队列取消路径建立清理回调，再销毁旧流水线。受控回归先在未插桩构建中复现队列停滞，再验证替换任务正常进入终态。

另一处缺陷是手动推理沿用了仅考虑自动推理和播放状态的声学准入，关闭自动推理时会停在等待状态。修复为目标流水线携带本次请求许可，variance 更新及声学缓存探测共用准入判断；进入 Ready 或取消时清除许可，不改变用户设置。受控生产状态用例验证后台等待、手动放行和完成后的策略恢复，真实资源流程验证完整输出。

GUI 测试通过真实控件事件检查界面和业务状态，覆盖快捷键焦点、菜单、分隔条、视口及生产动画等组件。纯数值或几何检查与真实输入路径分开，不以调用 Facade 后检查模型替代 GUI 测试。

## 4. 跨平台与资源条件

`lite_register_test` 集中配置测试类别、offscreen、原生桌面条件、资源条件和 Windows 动态库路径。通用测试不依赖 CI 环境变量，不无条件指定 Windows Qt 插件；原生桌面用例保留必要互斥。常规 Windows/MSVC 测试不要求覆盖率工具。

`ProcessFixture` 为场景分配数据根、配置、访问根和素材，只管理自身创建的进程与文件。成功清理，失败保留参数、退出状态、协议记录和 stdout/stderr。配置、日志、缓存和单实例身份复用统一数据路径，`DSEL_TEST_DATA_ROOT` 仅在测试构建生效，不扩展产品 CLI 或公开工具。

可执行文件路径来自 CMake target，stdio 阻塞接收端采用跨平台替身。重启进程使用独立输出文件，并具备各平台的存活、所有权和清理实现。Qt Test 的 Windows 日志进入可捕获输出，资源跳过与失败细节可以汇总。

显式资源用例已在 Windows 使用本机 Qixuan 声库实际加载模型，完成 CPU 推理、声码器和 WAV 导出，并检查解码结果与有效音频。未提供资源报告跳过，配置后加载或执行失败报告失败。该流程不要求播放设备，GPU、实际设备播放和听感的验收边界见测试报告。

常规 Headless 与声库测试共用 Native HTTP 请求、超时及响应校验，补齐实际协议要求的响应类型协商；资源用例提供显式语言和任务作用域，并等待异步 G2P/分段完成后再启动手动推理。共同传输路径在无声库测试中也执行，避免资源客户端独自偏离接口要求。

## 5. Linux CI 与覆盖率

Linux CI 使用单一 job 分段完成环境、依赖、配置、完整构建、测试、覆盖率和产物收集。面向 `main` 的 PR 更新及 `main` 推送触发验证；Qt、vcpkg 和依赖版本由 workflow、manifest 及 overlay 声明。完整构建失败或测试失败均保留失败退出码，并收集诊断材料。

缓存限于 Qt 和 vcpkg 包，不复用整个 CMake 构建目录。vcpkg 缓存键关联 Qt 和依赖声明，回退缓存仅提供候选包；每轮仍执行 install，由 ABI 决定复用或重建。失败构建使用独立 partial key 保存已完成包，完整成功缓存使用独立键。上游修复落地后，临时项目覆盖端口及其注册、失效哈希引用已移除。

冷缓存验收确认依赖从未命中状态完整安装；暖缓存验收确认实际命中后仍完成 install、构建和测试；依赖升级验证确认旧缓存可复用兼容包并重建变化的依赖。这些结论来自真实运行，不以删除缓存或命中提示代替完整验证。

GCC/gcov 插桩由显式选项启用，gcovr 统计项目生产源码的行与分支覆盖，排除测试、第三方及生成文件，不设百分比门槛。HTML、JSON、CSV、JUnit、完整测试日志和环境信息进入 CI 产物；逐文件 CSV 同时输出到日志，便于直接定位缺口。失败素材包含测试产物范围内的隐藏暂存文件。

Windows 使用独立 `coverage` preset 和 Visual Studio 原生静态插桩，构建时提供 PDB 与 `/PROFILE`，关闭与其不兼容的 Edit and Continue。采集器在执行前插桩，覆盖 Editor/Connector 子进程并恢复原二进制；项目脚本按源码文件和行合并多个程序中的重复统计。Windows 与 Linux 的编译代码、行映射不同，分别报告，不直接把百分比差值视为新增覆盖；Microsoft 输出中的占位分支比例不作为分支覆盖率。

## 6. 取舍与文档边界

测试目标按组件依赖和执行环境划分，后续可按共享 fixture 和依赖收益继续调整，不要求维持当前数量。推理门控使用真实任务快照验证结果应用规则，实际模型输出交给资源用例。主观视觉、实际听感和设备组合不建立像素或硬件矩阵。

默认钢琴窗和轨道视图走 Legacy/QGraphicsView；实验 RHI 后端需要显式启用并重启。现有真实输入用例覆盖默认路径，几何和字形组件测试不代表完整 RHI 控件交互、绘制或失败恢复已经验证；本期保留该实验范围，不为统计数值扩展图形设备测试。

本期文档是实施与发布验收材料，覆盖率详细计数和逐次运行记录留在 [Actions](https://github.com/flutydeer/ds-editor-lite/actions/workflows/tests.yml) 产物中，不建设长期文档同步或自动生成机制。最终通过依据包括规定测试与 CI、真实审查问题的处理，以及 Codex bot 对受审版本的认可；动态审查状态和完成证据统一保留在 [PR #187](https://github.com/flutydeer/ds-editor-lite/pull/187)。

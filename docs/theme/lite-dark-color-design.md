# Lite Dark 主题 neutral 色阶设计方案

> 本文档记录 lite-dark（及对照 lite-light）主题 `neutral` 数字系列（1000~0）的
> OKLCH 配色配方、设计动机与调整方法。下次调整配色前先读这里，避免重新摸索。

## 1. 背景与目标

- `neutral` 数字系列是一条**亮度阶梯**：编号 1000 = 最暗、0 = 最亮。
  编号只是**排序键**，不是百分比，也不跨主题共享数值（dark.500 ≠ light.500 的亮度）。
- 旧版问题：lite-dark 的 neutral 是手调 hex（彩度不可控、噪声大），lite-light 是另一套
  oklch 配方，两套主题风格不统一。
- 设计目标：两套主题共用**同一条 OKLCH 曲线**（色相 265° 微蓝调，与 accent 和谐），
  只取不同的亮度区间。彩度上两套各在自己"常用端"做二次衰减：dark 降暗侧
  （§2.3）、light 降亮侧（§2.4），方向相反但形状对称。

## 2. 核心配方

### 2.1 色相 H：恒定 265°

微蓝调的"中性色"，与 accent 蓝色系（#9BBAFF 等）同色相，比纯灰更和谐。

### 2.2 亮度 L：等差阶梯（步长 0.04）

| 主题 | 公式 | 区间 |
|---|---|---|
| lite-dark | `L(n) = 0.10 + (1000−n)/50 × 0.04` | 0.10 ~ 0.90 |
| lite-light | `L(n) = 0.18 + (1000−n)/50 × 0.04` | 0.18 ~ 0.98 |

每套主题各舍弃原 23 级阶梯两端的 2 级：dark 用不到极亮（亮端由 `neutral.0` 白色语义兜底），
light 用不到极暗（暗端由文本色覆盖）。21 级正好对应 `neutral.1000` ~ `neutral.0` 共 21 个位置。

### 2.3 彩度 C：暗端非线性衰减（关键设计）

- **亮侧**（n ≤ 500，即 450~0）：C 恒为 **0.015**。
- **暗侧**（n ≥ 550，即 550~1000）：二次曲线衰减：

```
C(n) = 0.015 − 0.000176 × ((n − 500) / 100)²
```

- 锚点：`neutral.500` → C = 0.015；`neutral.1000` → C = 0.0106。
- 系数来源：`k = (0.015 − 0.0106) / 5² = 0.000176`。

**为什么非线性**：暗色 + 高彩度会显脏（浑浊感）。曲线让"越暗、彩度下降越快"——
每 100 编号的降幅从亮端约 0.0002 递增到暗端约 0.0016，亮端几乎保持原样，
暗端才显著降彩。这还原了最初手调 hex 时的意图（反推原始值发现暗端彩度整体偏低，
但手调 hex 时彩度不可控、分布噪声大）。

**演进历史**（快速回顾，避免重复试错）：

1. 全阶梯恒定 C=0.015 → 发现暗端显脏；
2. 尝试暗端线性降 C：每 50 降 0.0005（太陡）→ 0.0004（视觉合适）→ 0.0003（太缓）；
3. 最终改用平滑二次曲线（锚点 1000 取手调认可的 0.0106），无折点、越暗越陡。

### 2.4 lite-light 亮侧彩度衰减（2026-08 新增）

lite-light 的 neutral 亮侧（编号 ≤500）不再恒定 C=0.015，改为与 dark 暗侧对称的
二次衰减，**越靠近亮端（编号越小）降幅越大**：

```
C(n) = 0.015 − 0.001467 × ((500 − n) / 500)²
```

- 锚点：`neutral.500` → C = 0.015；`neutral.0` → C = **0.0135**（0.001467 ≈ 0.0044 / 3）。
- 覆盖范围：编号 400 ~ 0（500 保留 0.015）。编号 550 及以上的暗侧维持 0.015 不变。
- **动机**：light 实际大量使用的是亮侧（面板/卡片/表面底色 0~300）。亮侧保持
  0.015 时最亮面板偏蓝偏"艳"；降彩后更素净。

**演进历史**（避免重复试错）：

1. 完整镜像 dark（`0.015 − 0.0044×((500−n)/500)²`，锚点 0 = 0.0106）→ 用户反馈降太狠；
2. 衰减减半（`0.0022`，锚点 0 = 0.0128）→ 仍偏狠；
3. **定稿** 衰减取 1/3（`0.001467`，锚点 0 = 0.0135），用户认可。公式系数 `0.001467 = 0.0044 / 3`（暗侧同源降幅的 1/3）。

### 2.5 lite-light 亮侧亮度非线性（2026-08 新增）

lite-light 亮侧（编号 0~500）不再等差（每档 -4%），改为非线性幂曲线，
**越靠近亮端（编号越小）每档亮度差越小、层次越密**：

```
L(n) = 0.58 + 0.40 × ((500 − n) / 500) ^ p   (p = 0.7)
```

- 锚点：`neutral.500` → L = 58%；`neutral.0` → L = 98%（两端不变，只改中间档）。
- p=0.7：50↔0 差 3%（原等差 4%），向下每档差距逐步拉大至暗端。
- **动机**：原等差下 `neutral.0` 与 `neutral.50` 虽只差 4% L，但在最亮区段视觉差异
  明显。亮端压缩后，背景（0/50）与前景更接近，层次靠 100~450 展开。
- 文本色（850~1000）与暗端完全不受影响，本节只改亮侧 10 个编号的 L。

**演进历史**（避免重复试错）：

1. p=0.5（50↔0 差 2%）→ 亮端过于挤、层次不足；
2. **定稿** p=0.7（50↔0 差 3%）→ 用户认可（过渡更平滑）。

> 备注：`piano.roll.blackRow` 原为用户手调写死 `oklch(95% 0.0138 265)`，后在明度定为
> p=0.7 时改为引用 `{neutral.50}`（值相同），保持 token 化维护。

## 3. 色值总表

### 3.1 lite-dark（`src/app/Resources/theme/lite-dark/colors.json`）

| neutral | oklch | sRGB hex |
|---|---|---|
| 1000 | `oklch(10% 0.0106 265)` | #020306 |
| 950 | `oklch(14% 0.0114 265)` | #07090E |
| 900 | `oklch(18% 0.0122 265)` | #0F1217 |
| 850 | `oklch(22% 0.0128 265)` | #181B21 |
| 800 | `oklch(26% 0.0134 265)` | #21242B |
| 750 | `oklch(30% 0.0139 265)` | #2A2E35 |
| 700 | `oklch(34% 0.0143 265)` | #343840 |
| 650 | `oklch(38% 0.0146 265)` | #3F424A |
| 600 | `oklch(42% 0.0148 265)` | #494D55 |
| 550 | `oklch(46% 0.015 265)` | #545861 |
| 500 | `oklch(50% 0.015 265)` | #5F636C |
| 450 | `oklch(54% 0.015 265)` | #6A6F78 |
| 400 | `oklch(58% 0.015 265)` | #767A84 |
| 350 | `oklch(62% 0.015 265)` | #828690 |
| 300 | `oklch(66% 0.015 265)` | #8E929C |
| 250 | `oklch(70% 0.015 265)` | #9A9EA8 |
| 200 | `oklch(74% 0.015 265)` | #A6ABB5 |
| 150 | `oklch(78% 0.015 265)` | #B3B7C1 |
| 100 | `oklch(82% 0.015 265)` | #BFC4CE |
| 50 | `oklch(86% 0.015 265)` | #CCD1DB |
| 0 | `oklch(90% 0.015 265)` | #D9DEE8 |

### 3.2 lite-light（对照，`src/app/Resources/theme/lite-light/colors.json`）

| neutral | oklch | sRGB hex |
|---|---|---|
| 1000 | `oklch(18% 0.015 265)` | #0E1218 |
| 950 | `oklch(22% 0.015 265)` | #171B22 |
| 900 | `oklch(26% 0.015 265)` | #20242B |
| 850 | `oklch(30% 0.015 265)` | #2A2E36 |
| 800 | `oklch(34% 0.015 265)` | #343840 |
| 750 | `oklch(38% 0.015 265)` | #3E424B |
| 700 | `oklch(42% 0.015 265)` | #494D56 |
| 650 | `oklch(46% 0.015 265)` | #545861 |
| 600 | `oklch(50% 0.015 265)` | #5F636C |
| 550 | `oklch(54% 0.015 265)` | #6A6F78 |
| 500 | `oklch(58% 0.015 265)` | #767A84 |
| 450 | `oklch(62% 0.015 265)` | #828690 |
| 400 | `oklch(66% 0.0149 265)` | #8E929C |
| 350 | `oklch(70% 0.0149 265)` | #9A9EA8 |
| 300 | `oklch(74% 0.0148 265)` | #A6ABB4 |
| 250 | `oklch(78% 0.0146 265)` | #B3B7C1 |
| 200 | `oklch(82% 0.0145 265)` | #BFC4CE |
| 150 | `oklch(86% 0.0143 265)` | #CCD1DB |
| 100 | `oklch(90% 0.0141 265)` | #D9DEE8 |
| 50 | `oklch(94% 0.0138 265)` | #E7EBF5 |
| 0 | `oklch(98% 0.0135 265)` | #F5F8FF |

## 4. whiteXX / blackXX 叠色系列（约定）

- 后缀 = **alpha 百分比**（四舍五入）：`white06` = #10FFFFFF = 16/255 ≈ 6%，`black13` = #20000000 ≈ 13%。
- 数字系列回答"这个面是什么颜色"（不透明、绝对）；白黑系列回答"我往底下叠多少"（半透明、相对）。
- 状态色（hover/pressed/disabled）、阴影、遮罩用白黑系列，天然适配任意底色（含业务色）。
- 主题极性：dark 用白叠（`control.fill.hover` = white10），light 用黑叠（= black08）。
- 两套主题按需声明，不要求对称（whiteXX 两套全共享，blackXX light 声明更多）。

## 5. ⚠️ 注意事项

1. **`neutral.0` 不再是纯白**：dark 的 `neutral.0` = oklch(90%) ≈ **#D9DEE8**（light 为 #F5F8FF）。
   引用它的 token：`text.emphasis`、`curve.edited`、`scrollbar.handleEmphasis`、
   `window.closeButton.foreground`、`slider.thumb.borderStrong`、`speakerMix.dividerDragging`。
   若需恢复纯白：暗端阶梯裁到 0.86 结束，`neutral.0` 单独写 `#FFFFFF`。
2. **gamut mapping 是预期行为**：L=0.98 时蓝色通道线性值 ≈1.036 超出 sRGB 色域，
   解析器（`ColorUtils::oklchToSRGB`）自动二分压缩彩度，得到 #F5F8FF 而非直转的 #F3F9FF。
3. **编号不跨主题对应**：同编号只保证相对明暗顺序一致，具体亮度/彩度按各主题配方取值。
4. **文件格式**：数字系列写 `oklch(L% C H)`；whiteXX/blackXX 写 `#AARRGGBB`。

## 6. 生成工具 LitePalette

命令行工具（`src/tools/LitePalette`），直接复用 `ColorUtils`（含 gamut mapping），
输出与主题加载器完全一致。

```bash
# 生成 23 级恒定 C 阶梯（0.10 起，步长 0.04）
LitePalette --C 0.015 --H 265 --L0 0.1 --step 0.04

# JSON 数组输出（L/C/H/oklch/hex 字段）
LitePalette --C 0.015 --H 265 --L0 0.1 --step 0.04 --json

# 指定数量
LitePalette --C 0.015 --H 265 --L0 0.1 --step 0.04 --count 21
```

参数：`--C`（彩度）、`--H`（色相）、`--L0`（起始亮度）、`--step`（亮度步长）、
`--count`（数量，默认直到 L 超过 1.0）、`--json`。

**注意**：CLI 目前只生成恒定 C 的阶梯；暗端二次衰减需按第 2.3 节公式计算
（或将来给 CLI 增加 `--chroma-slope` 之类的参数）。

## 7. 调整指南

- **改暗端衰减强度**：改系数 `0.000176`（k = (0.015 − C₍₁₀₀₀₎) / 25，C₍₁₀₀₀₎ 是期望的暗端彩度）。
  k 越大衰减越陡。
- **改回线性衰减**：`C(n) = 0.015 − 0.0008 × ((n−500)/100)`（即每 50 降 0.0004，历史"合适"档）。
- **改亮度区间**：改 L0 与步长，保持 21 级 ↔ 21 位置（1000~0）的对应关系。
